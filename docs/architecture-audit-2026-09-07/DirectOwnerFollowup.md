# Direct-owner follow-up after #1350

## Scope and acceptance criteria

Based on merged develop `e0575490d`. Continue Chloe-JY’s earlier cleanup and the Harry19081 follow-up in #1350. Publish this follow-up as Harry19081 after user authorization. Remove redundant internal forwarding steps, preserve production bodies and intentional public exports, migrate mocks, verify references and run affected tests. Large state facades remain deferred.

The pre-edit compiler-API scan covered 6,592 TS/JS files: 396 files with module re-exports and 228 pure re-export files. Counts are inventory, not proof that every barrel is unnecessary. Static import/export, literal dynamic import/require, import types and supported mock references were resolved with the repository tsconfig.

## Findings and dispositions

| Line                                                                                                  | Element                         | Verdict          | Reason                                                                                                                                                  | Suggested change                                                                                  |
| ----------------------------------------------------------------------------------------------------- | ------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `src/engines/Simulator/exports.ts:4`                                                                  | Simulator intermediate API      | fix              | Only its index consumes it, requesting ActivitySimulator                                                                                                | Removed; index exports directly from ActivitySimulator                                            |
| `src/scaffold/ContextMenu/variants/index.ts:7`                                                        | Variant forwarding layer        | fix              | Only consumed by the public ContextMenu exports                                                                                                         | Removed; public exports point to component/hook owners                                            |
| `src/scaffold/ContextMenu/variants/TextSelectionDropdown/exports.ts:5`                                | Second variant forwarding layer | fix              | Only consumed by the preceding intermediate layer; config/types are not forwarded by that layer                                                         | Removed; preserve the existing public component/hook surface                                      |
| `src/scaffold/GlobalSpotlight/palettes/core/index.ts:7`                                               | Selector alias forwarding       | fix              | 17 incoming references to a hook owned elsewhere                                                                                                        | Removed; direct imports retain useSelectorKernel as a local alias, and mocks use the owner export |
| `src/modules/MainApp/Settings/subpages/BackgroundPage/hooks/index.ts:1`                               | Hook barrel                     | fix              | One consumer and one owner                                                                                                                              | Removed; import useBackgroundSettings directly                                                    |
| `src/modules/MainApp/Settings/subpages/BackgroundPage/components/index.ts:1`                          | Component barrel                | fix              | One consumer and one owner                                                                                                                              | Removed; import ColorSection directly                                                             |
| `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/CodeViewerContent/hooks/index.ts:4` | Hook barrel                     | fix              | One consumer and one owner                                                                                                                              | Removed; import useCodeViewerHandlers directly                                                    |
| `src/modules/MainApp/Integrations/DevTools/playground/single-event/index.ts:1`                        | Preview barrel                  | fix              | One consumer and one owner                                                                                                                              | Removed; import SingleEventPreview directly                                                       |
| `src/scaffold/NavigationSidebar/contexts/index.ts:4`                                                  | Context barrel                  | fix              | One incoming import and one context owner                                                                                                               | Removed; import ForceVisibleContext bindings directly                                             |
| `src/engines/SessionCore/workspace/hooks/index.ts:11`                                                 | Workspace-hook barrel           | fix              | Only the public SessionCore API consumes it                                                                                                             | Removed; public API exports useRepositoryInfo directly                                            |
| `src/store/config/index.ts:6`                                                                         | Unused configuration entry      | fix              | Zero resolved consumers and no raw source/config/tool references to the entry                                                                           | Removed; configAtom remains unchanged                                                             |
| `src/store/platform/index.ts:5`                                                                       | Unused platform entry           | fix              | Zero resolved consumers and no raw source/config/tool references to the entry                                                                           | Removed; underlying state modules remain unchanged                                                |
| `src/assets/providers/types.ts:9`                                                                     | Provider type forwarding step   | fix              | Public provider API and one hook can use canonical key types directly                                                                                   | Removed type forwarding only; keep ORGII_ORCHESTRATOR and the public provider API                 |
| `src/modules/ProjectManager/shared/index.ts:6`                                                        | Shared component API            | keep with reason | Cohesive shared UI API used by 25 references, including mocks; flattening the outer layer alone merely shifts the same boundary                         | Retain pending a separate ownership justification                                                 |
| `src/hooks/logger/index.ts:1`                                                                         | Logging hook API                | keep with reason | Stable, named subsystem entry used broadly; a single implementation alone does not establish redundancy                                                 | Retain                                                                                            |
| `src/store/ui/chatPanelAtom.ts:1`                                                                     | Large state facade family       | keep with reason | Explicitly postponed by the user; state ownership/evaluation requires a separate migration                                                              | Defer                                                                                             |
| Rust forwarding-only files                                                                            | Five intentional boundaries     | keep with reason | Connection dependency boundary, command registry, table enum aliases, public tool names and session filter API retain their previously documented roles | No Rust edits                                                                                     |

## Architecture and lifecycle coverage

Covered canonical ownership/naming, dead indirection, module boundaries and reference tracing (architecture layers 1–4 and 6–7). No executable behavior, fallback logic, initialization, wire shapes or resolver algorithms changed (layers 5 and 8–10); broader audits of those systems are outside this migration. TSX edits change imports only, so the frontend UI audit’s architecture/type exclusion applies. No timers, subscriptions, caches, state implementations or streaming logic changed; no runtime performance improvement is claimed.

Removed paths are source-level compatibility changes; out-of-tree consumers must migrate. Literal-reference scanning cannot establish liveness of arbitrary nonliteral loaders. Screenshots are unnecessary because JSX and styles are unchanged. GUI/E2E, release bundling and Rust checks are outside this TypeScript-only batch.

## Verification

- `pnpm typecheck:fast` — passed.
- ESLint (`--fix --max-warnings 0`) and Prettier — passed on all 27 modified TypeScript source/test files.
- `pnpm test src/scaffold/GlobalSpotlight src/features/SessionCreator src/scaffold/ContextMenu src/engines/Simulator src/modules/MainApp/Settings/subpages/BackgroundPage src/modules/MainApp/Integrations/DevTools src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/CodeViewerContent src/scaffold/NavigationSidebar src/engines/SessionCore/workspace src/assets/providers src/hooks/config src/app/root/__tests__` — 157 files / 850 tests passed.
- Compiler-API reference sweep — 12 deleted modules, zero remaining resolved references. Production AST comparison — 25 modified production files, zero executable declaration/statement changes.
- `pnpm check:circular` — zero cycles across 6,556 modules.
- `pnpm check:test-placement` — passed across 510 directories.
- `git diff --check` — passed.
- Post-edit inventory: 6,580 TS/JS files, 383 with module re-exports, 216 pure forwarding files. Remaining entries are not automatically unnecessary.
- Full frontend suite not rerun; verification covers affected modules and startup/worker boundaries. No Rust source changes. Publication uses Harry19081; the commit and PR identify this as follow-up work to Chloe-JY and #1350.
