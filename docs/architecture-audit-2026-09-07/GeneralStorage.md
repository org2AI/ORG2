# General settings consolidation

Acceptance: Storage is General tab four; HTTP version renders in General and search resolves there; resource/network settings and their frontend owners are removed; old links resolve; shared diagnostics remain intact.

| Layer                | Result                                                                                                                                                                                                                                               |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 Compilation        | Typecheck and targeted lint run; see performance report for final verification                                                                                                                                                                       |
| 2 Dead code          | Removed MonitorSection, NetworkSection, useMonitorMetrics, useNetworkSectionData, useNetworkMonitor, slot/manifest/navigation entries, and obsolete refresh/scanning atoms. Storage's unreachable external-refresh callback removed with its trigger |
| 3 Naming             | No remaining frontend references to removed components/hooks/atoms. Existing monitor translation namespace stays because Storage, HTTP preferences, and sidebar RAM use it                                                                           |
| 4 Semantics          | Settings diagnostics and shared runtime diagnostics are distinct. Shared RAM snapshot code supports sidebar/runtime consumers; region lookup supports useRegionCheck                                                                                 |
| 5 Defaults           | Old monitor/storage resolves to General/Storage; other retired monitor paths resolve to General/General                                                                                                                                              |
| 6 Boundaries         | General owns the existing HTTP block; settings persistence still owns network.httpVersion                                                                                                                                                            |
| 7 Clarity            | Registry and search advertise only surviving settings; Spotlight points to Storage                                                                                                                                                                   |
| 8 Wire               | Removed the two unused internal VPN/tool-process diagnostic commands; see SettingsSamplingCleanup.md for backend evidence. Surviving preference formats and HTTP behavior are unchanged                                                              |
| 9 Init               | Navigation and search use the same General routes. Storage mounts directly, without the removed monitor owner                                                                                                                                        |
| 10 Resolver symmetry | Section and tab parsers both map retired monitor URLs to General; no data fallback chain changed                                                                                                                                                     |

Frontend ownership/routes reviewed across all ten layers. Backend removals are reviewed in SettingsSamplingCleanup.md. Live protocol and multi-instance checks were not run. No migration or historical data cleanup required. Existing unrelated working-tree changes preserved.
