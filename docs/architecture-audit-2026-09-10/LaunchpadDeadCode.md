# Launchpad dead-code audit

The four cleanup groups below are implemented on a branch from origin/develop. The right-side spacing fix is a separate PR. Audit-time line references and evidence are retained below; source changes preserve the existing launch contracts.

Completion criteria: trace the current Launchpad entry points, distinguish unused exports from dead implementations, inspect local helper arguments that export scanners miss, and record verification without claiming runtime savings.

## Findings

| Line                                                                | Element                                           | Verdict          | Reason                                                                                                                                                                                                                                                                                              | Suggested change                                                                                                                                                     |
| ------------------------------------------------------------------- | ------------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/engines/ChatPanel/ChatPanelEmptyContent.tsx:225`               | `renderSessionLauncher` general-purpose arguments | fix              | Both call sites (310 and 371) pass layout `launchpad`, footer `undefined`, and multi-runner `true`; neither supplies the fifth argument. The default-layout, footer customization, non-parallel, and hide-attachment options have no callers in this local helper.                                  | Rename to `renderParallelRunLauncher`, retain the varying class name, and inline the existing constant props. Preserve the separate live Session/Work Item composer. |
| `src/modules/WorkStation/AppShell/StartPage/index.tsx:37`           | `StartActionRow.shortcut`                         | fix              | This private component is only rendered twice in the same file. Both callers supply `shortcutId`; neither supplies `shortcut`. The raw-shortcut input and its conditional branch are unused.                                                                                                        | Remove the optional prop, destructuring, and forwarding; condition only on `shortcutId`.                                                                             |
| `src/modules/shared/launchpad/components/index.ts:1`                | Four re-exports                                   | fix              | `ContainerEnginesSection`, `ContainersSection`, `LaunchpadActionStrip`, and `MacFolderIcon` have no consumers through this barrel. Their implementations are used through direct imports.                                                                                                           | Remove these four re-exports only.                                                                                                                                   |
| `src/modules/shared/launchpad/components/RepoDetail/index.ts:1`     | Six re-exports                                    | fix              | `AddEnvVarRow`, `AddScriptRow`, `CATEGORY_OPTIONS`, `STATUS_DOT_COLOR`, `STATUS_LABEL_KEY`, and `STATUS_TEXT_COLOR` are consumed directly from their defining files, not this barrel.                                                                                                               | Remove these six re-exports; retain `RepoDetailPage` and the implementation files.                                                                                   |
| `src/modules/shared/launchpad/components/LaunchpadDashboard.tsx:71` | Older-named dashboard and dependencies            | keep with reason | Runtime Assets dispatches `WorkspaceDashboardPanelView` through a lazy import in `modules/shared/dataSource/index.ts`; that view mounts the dashboard. Repo detail is also mounted by `WorkspaceOverviewPanelView`.                                                                                 | Keep. The older Launchpad name does not establish dead code.                                                                                                         |
| `src/modules/WorkStation/AppShell/StartPage/index.tsx:91`           | WorkStation start page                            | keep with reason | Mounted by both `AppShellContent` and the start-tab renderer. Its launch actions are shared with the live plus menu.                                                                                                                                                                                | Keep.                                                                                                                                                                |
| `src/engines/ChatPanel/ChatPanelEmptyContent.tsx:298`               | Session, Work Item, More composition              | keep with reason | ChatPanel creation content renders the start page, which selects the stable agent composer, manual work-item creator, project creator, or parallel launcher. These are different launch contracts, not duplicate dead components. Non-start-page branches are not proven unreachable by this audit. | Keep unless a separate surface-state trace proves a branch unreachable.                                                                                              |

Four cleanup groups: one overspecified local helper, one unused local prop, and ten unused re-exports across two barrels. No whole dead Launchpad component was confirmed.

## Evidence and verification

- `pnpm exec knip --include files,exports,types --reporter json`: completed with exit 1 because findings exist repository-wide. Launchpad matches were the ten barrel exports above. Scanner results were checked against direct imports and production entry points.
- `pnpm run typecheck:fast`: passed, exit 0.
- Source sweep covered `src/modules/shared/launchpad`, the workstation start page, ChatPanel start/empty content and creation hooks, SessionCreator Launchpad actions, and their callers throughout `src`.
- Audit-time verification did not include runtime tests or GUI checks. Cleanup verification is recorded in the PR. No CPU, memory, or bundle improvement is claimed. Recheck callers before applying cleanup because the shared working tree contains concurrent changes.

## Architecture coverage

1. Compilation: TypeScript fast typecheck passed.
2. Reachability: production import/render chains and local function call sites inspected.
3. Naming: historical dashboard naming distinguished from dead code.
4. Semantic overloading: Launchpad means the chat creator, workstation empty state, and older workspace dashboard; each has a live entry point.
5. Defaults: identified unused helper defaults; retained create-target fallback behavior.
6. Boundaries: shared dashboard dependencies retain live consumers; unused barrel exposure is distinct from unused implementation.
7. Readability: helper name implies broader Session launching than its parallel-only callers support.
8. Wire protocol: not applicable to this local reachability audit; no payload or serialization changes or backend audit performed.
9. Entry parity: traced chat creation, workstation empty/start-tab, Runtime Assets, and workspace details separately; no initialization changes proposed.
10. Resolver symmetry: no multi-source resolver changes proposed; persistence and launch-context resolution are outside this dead-code pass.
