# Agent Org 后台资源与执行历史实施记录

状态：后台资源阶段的自动与桌面渲染检查已通过；执行历史阶段和最终真实 provider 联合验收尚未完成。本文中的“通过”只指实际执行过的对应检查；生产代码继续变化时，旧结果不作为最终构建证据。

## 基线与顺序

基线为前置 PR #2105 的 `6968a34d529c5e4c627e91b647bdd38b17139075`，工作分支为 `codex/agent-org-background-history`，使用独立工作区。没有合并或变基到 develop，也没有修改原工作区的已暂存文档。先完成后台资源阶段及其测试，再实施执行历史阶段，最终交付一个面向 develop 的完整堆叠 PR。

最终真机验收使用新的隔离数据库、项目和 BuildFast 应用实例，选择 `codexharry` 的 `gpt-5.6-luna-medium`。真实 provider、联合验收、65 分钟运行和 Windows 编译目前均未完成；Windows 真机依照范围约定保留待验。

## 资源契约

| 用户结果／契约                   | 正式负责位置                                                    | 入口及状态                               | 确定性证明                                                 | 取消、迟到及失败处理                            | 真机证明 |
| -------------------------------- | --------------------------------------------------------------- | ---------------------------------------- | ---------------------------------------------------------- | ----------------------------------------------- | -------- |
| 后台服务不阻止任务及回复正常结束 | 进程登记的资源策略；执行器完成检查                              | shell 已返回句柄；后台子 agent 仍须完成  | 登记回归已先红后绿；执行器工具边界测试已补                 | 失败和显式取消仍等待精确执行的资源              | 待验     |
| 不用 PID 充当进程身份            | 每次启动的唯一登记编号；原 session/call/intent/lease/generation | 启动、后台化、查询、单进程 Stop          | 同 PID、同 call 的过期请求和迟到回调                       | 不匹配即拒绝，不回退到直接按 PID 杀进程         | 待验     |
| 启动与清理没有归属空档           | 同步与后台 subprocess 的登记入口；已有任务写入屏障              | 先登记，再发布进程事件或释放屏障         | 取消登记、前台转后台竞争                                   | 登记拒绝则回收已经启动的进程                    | 待验     |
| 新轮 Stop 保留旧轮服务           | 后端精确 owner；前端 Org Stop 不再全会话扫杀                    | 普通 Stop、按 intent Stop、进程卡片      | 真实 HTTP 服务＋新轮 Stop 已通过                           | 普通会话 Stop／Force Send 保持原范围            | 待验     |
| 替换任务前旧写者确实停止         | 不可变 run/task 关联；已有交接与任务屏障                        | 活跃任务交接、已结束任务的明确替代       | 真实文件写入进程在释放后不再污染替代文件，已通过           | timeout 保留失败证明，不靠删除登记行宣布成功    | 待验     |
| 暂停只清理受影响工作             | 暂停时的未完成任务与精确 owner 快照                             | Pause／Resume；既有执行让出流程          | 正式存储边界回归已通过：暂停／成员让出保留已完成任务的服务 | Resume 不能绕过旧资源清理，迟到快照不捕获新执行 | 待验     |
| 退出包含空闲会话的服务           | 应用关闭入口及本应用 registry                                   | 先关闭登记，再排空执行和资源，最后数据库 | 独立测试进程中的实际 HTTP 服务退出与拒绝新登记，已通过     | 有限等待和终止升级；未知状态不伪装释放          | 待验     |
| 日志退出不启动新模型轮           | 后台监控及原 session/call 回放                                  | 正常退出、异常退出、输出失败             | 后台退出不进入普通提醒／唤醒，已有回归通过                 | 仅推进已经存在的交接等待                        | 待验     |
| 保持有界内存                     | 原有输出队列与预览；终态登记按事件／访问回收                    | 存活、终止、历史查询                     | 原有内存回归通过；登记数量回归已补                         | 存活资源不按 TTL 淘汰；不新增收尾轮询           | 待验     |

完整磁盘日志继续保留，磁盘增长没有配额上限。不新增服务表、守护程序、自动重启、跨应用重启保活或历史清理。

## 已执行检查

| 检查                                                                                                | 实际结果及范围                                                                                      |
| --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 基线 `cargo test -p agent_core --lib job_registry_tests`                                            | 23 项通过                                                                                           |
| 新增“独立 Org shell 不阻止完成但保留归属”回归，旧实现                                               | 失败于“服务不应保持模型轮运行”的断言；取消与归属断言未被移除                                        |
| 同一回归，拆分策略后                                                                                | 通过                                                                                                |
| `cargo test -p agent_core --lib core::tools::impls::coding::exec`                                   | 中间实现 78 项通过、2 项原有专项测试忽略；之后的接线变化需重跑                                      |
| 真实 HTTP 服务回归                                                                                  | 旧服务可访问→新轮 Stop→旧服务仍可访问→单进程 Stop→端口关闭；通过                                    |
| 真实文件写入隔离回归                                                                                | 旧任务的后台写者退出后，新文件保持替代内容；无关任务进程仍运行；通过                                |
| 前端相关 Vitest                                                                                     | 最新 7 个文件、90 项通过，包含实际渲染 Stop 按钮到 IPC 的完整身份传递、Org Stop、迟到回调与未知状态 |
| `pnpm typecheck`                                                                                    | 默认 Node 堆上限不足导致工具内存退出；提高工具堆上限到 12 GiB 后通过，没有为此修改产品代码          |
| `cargo test -p agent_core --lib`                                                                    | 最新 3,715 项通过、3 项原有专项测试忽略                                                             |
| `cargo test -p agent_core --lib real_subprocess_ten_megabytes_is_complete_and_bounded -- --ignored` | 显式执行 10 MiB 真实子进程专项，1 项通过                                                            |
| `cargo test -p org2 --lib agent_sessions::event_pipeline`                                           | 324 项通过、1 项原有专项测试忽略                                                                    |
| `cargo check -p agent_core --all-targets`                                                           | 通过                                                                                                |
| `cargo clippy -p agent_core --all-targets -- -D warnings`                                           | 通过                                                                                                |
| `cargo clippy -p org2 --all-targets -- -D warnings`                                                 | 通过                                                                                                |
| `pnpm lint`、`pnpm check:circular`                                                                  | 通过；8,120 个模块未发现循环依赖                                                                    |
| `pnpm check:test-placement`、`pnpm check:i18n-keys`                                                 | 通过；国际化缺失、未用、语言差异和占位符问题均为零                                                  |

HTTP 测试初版暴露两个测试环境前提：冷启动 Python 超过两秒，以及独立测试未初始化应用的 TLS provider。改为有界启动等待与直接本机明文 HTTP 请求；未改变产品行为来迁就夹具。失败夹具遗留的已核实测试进程已清理。

## 架构检查与剩余工作

十层检查范围为：编译类型、实际调用链、名称职责、状态与终态语义、错误及缺省分支、角色权限、用户流程、Rust/TypeScript 协议、不同入口一致性、身份解析。以上层均与两项改动有关；不扩大到无关模块的整理。

当前代码把“属于 Org”与“阻止本轮完成”分开，新的生命周期与测试分别放在子模块。接口、暂停／退出回归、严格编译和 Clippy、前端静态检查已完成；尚需完成真实应用渲染 E2E、界面证据与性能实测。执行历史阶段尚未开始。

接口新增登记编号及对应校验，历史缺字段时不能据旧 PID 停止未知进程。未新增数据库表、迁移或依赖。最终回滚前须盘点仍在运行的本应用自有进程，并使用版本匹配的数据目录；不能重写任务终态或用户历史。

## 阶段核查补充

- 新增结束任务替换回归验证：替代任务创建必须等待上一轮的进程监控与输出边界释放，旧任务仍保持 Completed
- 正常退出回归运行于独立测试进程：关闭登记入口，清理 runtime 已释放的会话资源，再拒绝新启动；避免测试重置生产关闭开关
- 重启历史投影区分“未知”与“已退出”；后台登记身份丢失后，不依靠旧 PID 接管或停止进程。未知资源不再显示模型正在执行
- 日志持久化失败与进程释放分别记录：日志保持 incomplete；已证实进程树消失、读写任务已退出时，不保留虚假的活跃资源
- 前端 lint 与循环依赖检查已通过；测试位置检查指出新文件应遵循本目录同级放置规则，已移动。国际化检查无缺失键或语言缺口
- 新暂停测试夹具先后遗漏 Completed 的输出字段和完整输出类型；核对存储约束与正式类型后改为类型序列化，未改产品约束。应用接口测试首次构建缺少 org2-pm 配套产物，BuildFast 生成后重跑通过
- 当前生产增删约 1,434 行、测试约 1,200 行（PR5 阶段）；生产文件数超过初估，包含既有布尔名称、唯一登记字段、未知状态在 Rust/TypeScript 各消费者的机械接线，最终按职责与机械变更分别核算

性能结论仍为待完成：静态检查未新增保活轮询；真实桌面隐藏、长时间运行和清理后的进程／内存证据尚未获得。

## 桌面阶段验证记录

后台资源阶段使用带 WebDriver 的 BuildFast Instance 27，二进制 SHA-256 为 `04b46cfcfbda1f82abb81511d4a58f3092401842c29c830d832e67c06d06efe7`。在新建应用库、空 provider 种子和独立项目中运行，测试 provider 为 mock；这些结果不冒充最终指定真实 provider 的联合验收。

- 改派／取消：通过实际界面按钮操作，核对原登记身份、旧进程组退出、新登记身份和最终无残留资源，1 项通过
- 十轮暂停／恢复：正式执行持久状态和 9 组父子进程全部排空；暂停的 coordinator 请求被拒绝且草稿恢复；只派发一次续跑，1 项通过
- 后续请求／正式终态：空闲与忙碌后续请求、流式 Stop、工具 Stop、provider 失败和 panic 的正式终态，5 项通过
- 归档／删除：空闲、工作中和暂停团队经实际界面归档、删除，3 项通过
- 总结：首轮 4 项通过；另 1 项要求 info 级别唤醒记录，而环境设为 warn。补齐日志后重跑完整 5 项均通过；读回两次 NoReadyWork，未启动 provider、创建空 intent 或消费恢复预算
- 暂停路由的相邻单元测试：8 项通过；改动 TypeScript 文件长度检查通过，17 个文件均未超过 700 行

暂停 E2E 的两条旧断言与 #2105 已有产品契约不一致：旧横幅文本及“所有发送一律禁用”。已只读核对完整发送路由与基线单元测试，产品代码保持不变；测试改为实际点击未指定成员的正式请求，要求可见拒绝提示、草稿恢复、收信数量不变和团队仍暂停，不削弱正式工作冻结的验证。

反复启动同一测试数据目录时观察到 MCP 状态尚未初始化的启动 panic 日志；后续界面测试继续运行。尚未将其归因于本次更改，也未扩展修复；最终重启验收需单独核对。包安装最初因系统 CA 未提供给 Node 失败，使用系统证书后按原锁文件完成，没有更改依赖。

阶段结论：15 项桌面渲染 E2E 通过（交接 1、暂停 1、总结 5、后续请求与终态 5、归档删除 3）；全部使用同一 PR5 二进制。测试文案与暂停路由断言的修正不改变生产构建。最终 65 分钟、指定真实 provider、跨平台及完整 PR1—PR6 验收继续保留待验。

## 执行历史阶段进展

用户已明确接受将此阶段生产代码预算调整为约 1,100—1,300 行，以同时覆盖持久分页、实时投影和缓存卸载。范围仍限于执行历史，不新增业务表或服务管理体系。新增的 SQLite 表达式索引只用于按正式执行编号定位已有事件；升级会重建派生轮次缓存，不改写旧消息正文。界面翻译单列统计。

执行历史的正式来源为已有执行上下文、不可变团队快照、收信 materialization 回执和总结回执。写入时关联真实执行编号，同时保留收信批次的稳定消息编号与重放去重；系统处置不计入模型收信。真实执行开始记录保存在普通事件流中，作为没有输入／输出的总结轮的展示锚点，不建立第二套执行状态。旧事件缺失信息时保留原文并显示已知事实，未清理或猜补用户历史。

持久轮次索引、按轮加载、缓存卸载、实时投影和群聊导航使用同一个正式执行编号。迟到事件按所属编号归回原轮，同时间戳按持久顺序排序。导航只接受精确匹配的成员、会话和轮次；缺少目标时不跳到近似结果。新增索引与轮次缓存可重建；未新增业务表、依赖或服务状态。

已执行并通过：

- `cargo test -p agent_core -p session_persistence --lib`：核心库 3,718 项通过、3 项原有专项忽略；持久化库 64 项通过，包括真正 SQLite 的跨页、迟到、同时间戳、重建和无输出总结轮
- `cargo test -p org2 --lib agent_sessions::event_pipeline`：325 项通过、1 项原有专项忽略，覆盖正式执行缓存卸载与原事件身份
- `cargo clippy -p agent_core -p session_persistence -p org2 --all-targets -- -D warnings`：通过
- 前端相关 `pnpm test <paths>`：21 文件、176 项通过，覆盖来源解码、接口 schema 保留、实际详情按钮、收信展开、分页定位、普通会话和原有投影路径
- `NODE_OPTIONS=--max-old-space-size=12288 pnpm typecheck`、`pnpm lint`：通过
- `pnpm check:circular`：8,126 个模块通过；`pnpm check:test-placement` 和 `pnpm check:i18n-keys`：通过，国际化缺失／未用／语言差异／占位符均为零

尚未完成执行历史阶段的桌面验收、最终指定真实 provider 联合验收与长时间性能证据。

### 执行历史桌面阶段与基线对照

本阶段 BuildFast Instance 27 的二进制 SHA-256 为 `ab433551908cee3601f0fb81470bbed012af4cf56d0c50bdab003ea874a52b52`。生产源文件在本次构建后未修改；后续增加的是测试断言与记录。

第一批后续请求／正式终态 5 项和总结 5 项通过。群聊完整旧套件 3 项通过，其余出现失败，不能写成全部通过。已经用 #2105 原始提交构建的 Instance 28（二进制 SHA-256 `d55b1b74a198af4ef7e967c7a4c4ae6620211f72d159beb746810a984db7a947`）单独重跑其中三项：任务 FSM 旧场景、群聊提及前置 task_create、计划审批前置 task_create，均出现相同失败。前置任务的调试调用缺少当前正式执行权限，未放宽产品权限来迁就测试。完整旧套件另观察到拖选结果为空，以及 WebDriver 插件执行脚本登记 panic；尚不能将这些归因于此次更改。

新增的历史验收沿真实发送、精确群聊详情入口、页面刷新、成员切换和总结 Stop／Retry 完成，测试夹具不代写被验证的任务终态、消息处置或报告。旧套件失败与新行为验收分别记录，不以选择性测试冒充旧套件全绿。

Windows 交叉编译实际运行 `cargo check -p agent_core --all-targets --target x86_64-pc-windows-msvc`，在 ring 的 C 依赖处因缺少 Windows SDK／C 运行库头文件 `assert.h` 退出；这属于本机交叉工具链缺口，不能算 Windows 编译通过。Windows 编译与 Windows 进程树真机均保留 BLOCKED。macOS `cargo check -p agent_core --all-targets` 通过。

执行历史阶段新增的完整桌面检查已通过：后续请求与终态 5 项、总结 5 项；覆盖刷新后从只加载最新轮的窗口定位旧轮、无输出 Stop 总结标题、重试后的新标题，以及重启后的两份正式来源记录。生产代码增删 1,274 行，翻译 195 行；新增测试断言后测试体量另行核算。阶段完成，最终真实 provider、长运行和稳定性复跑仍待完成。
