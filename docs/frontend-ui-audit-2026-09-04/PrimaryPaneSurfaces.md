# Primary pane surfaces UI audit

| Line                                                              | Element                                  | Verdict          | Reason                                                                                                                                                                                                                          | Suggested change                                                                                          |
| ----------------------------------------------------------------- | ---------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `src/modules/shared/layouts/viewContainerTokens.ts:152`           | Primary pane host style                  | abstract         | Chat Panel, My Station, WorkStation-hosted chat, and detached sessions all need the same base paint and opacity behavior. The shared helper now owns that contract and rebinds existing semantic aliases at the host boundary.  | Keep new full-pane hosts on `getPrimaryPaneBackgroundStyle`; do not add component-level surface branches. |
| `src/modules/WorkStation/AppShell/index.tsx:137`                  | My Station surface boundary              | fix              | My Station previously inherited separate WorkStation, editor, and chat-pane values, which made shared pages and shell chrome disagree. The root now binds them to one primary pane paint while leaving Agent Station unchanged. | None.                                                                                                     |
| `src/config/workstation/tokens.ts:12`                             | Editor, sidebar, and placeholder classes | keep with reason | These names still describe the components that consume them. Their CSS variables are rebound by the host, so retaining the classes avoids a noisy site-by-site rewrite while producing one visual surface.                      | None.                                                                                                     |
| `src/engines/ChatPanel/index.tsx:141`                             | Chat Panel surface boundary              | keep with reason | The Chat Panel remains a primary-pane host and now consumes the generically named helper; shared descendants can keep their semantic `bg-chat-pane` classes.                                                                    | None.                                                                                                     |
| `src/modules/SessionWindow/index.tsx:95`                          | Detached session surface                 | keep with reason | A detached session is a standalone primary pane and needs the same solid-background opacity and descendant alias contract as in-app chat.                                                                                       | None.                                                                                                     |
| `src/modules/WorkStation/TabContent/renderers/chatSession.tsx:49` | WorkStation chat tab surface             | keep with reason | The embedded transcript remains semantically a chat surface, while the surrounding My Station host resolves all full-pane aliases to the same paint.                                                                            | None.                                                                                                     |

Verdict totals: **1 fix**, **4 keep with reason**, **1 abstract**.
