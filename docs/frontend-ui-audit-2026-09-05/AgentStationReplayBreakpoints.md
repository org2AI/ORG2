# Agent Station replay breakpoints UI audit

| Line                               | Element                               | Verdict          | Reason                                                                                                                                                                                                                                                                            | Suggested change |
| ---------------------------------- | ------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `ReplayProgressBar/index.tsx:87`   | Shared slider with breakpoint overlay | keep with reason | The scrubber continues to use the shared `Slider`; the sibling overlay is required to place interactive round boundaries without changing the shared slider API for unrelated consumers.                                                                                          | None.            |
| `ReplayTurnBreakpoints.tsx:34`     | Round-detail tooltip                  | keep with reason | Reuses the shared `Tooltip` and its smart placement rather than introducing custom hover content.                                                                                                                                                                                 | None.            |
| `ReplayTurnBreakpoints.tsx:41`     | Raw breakpoint button                 | keep with reason | The transparent 16 px absolute hit target and dynamically computed track position are specialized scrubber geometry that the generic `Button` chrome cannot express without disturbing the rail. It retains native button semantics, an accessible label, and current-step state. | None.            |
| `ReplayProgressBar/index.scss:41`  | Breakpoint marker dimensions          | keep with reason | The 16 px hit target and 6/7 px dots are deliberately coupled to the existing 1/2 px replay rail and do not correspond to a reusable application spacing or control token. Colors use theme variables.                                                                            | None.            |
| `SimulatorStatusBar/index.tsx:154` | Raw free-browse icon button           | keep with reason | This remains part of the status bar's established 20 px raw-button cluster; the shared button component adds geometry that would shift neighboring replay controls. The existing shared tooltip supplies the visible explanation.                                                 | None.            |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.
