# Chat event rows UI audit

| Line                                                               | Element                                  | Verdict          | Reason                                                                                                                                                                                                                                       | Suggested change                                                                                                                                                         |
| ------------------------------------------------------------------ | ---------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/engines/ChatPanel/blocks/primitives/EventBlockHeader.tsx:42`  | Composite clickable event header `<div>` | fix candidate    | Expandable rows remain mouse-only. Converting the current wrapper directly to a native button would create invalid nested controls around the Agent Station arrow and other header actions.                                                  | Split the header into a native expand trigger region plus a sibling action region, then add `aria-expanded` and keyboard activation in a dedicated accessibility change. |
| `src/engines/ChatPanel/blocks/primitives/EventNavigateIcon.tsx:57` | Raw navigation `<button>`                | keep with reason | This is the shared event-navigation primitive itself; its zero-width hover reveal and compact icon-only geometry are not expressed by the general design-system button API. It now has a default accessible label and normal keyboard focus. | None.                                                                                                                                                                    |
| `src/engines/ChatPanel/blocks/primitives/config.ts:284`            | Exact `h-[36px]` event-row metric        | keep with reason | The height is centralized once for the complete chat-event family and preserves the established timeline density; the sweep adds no per-consumer arbitrary sizes.                                                                            | None.                                                                                                                                                                    |

Verdict totals: **1 fix**, **2 keep with reason**, **0 abstract**.
