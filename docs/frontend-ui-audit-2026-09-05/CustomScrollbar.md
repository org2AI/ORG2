# CustomScrollbar UI audit

| Line                                           | Element                   | Verdict          | Reason                                                                                                                                                        | Suggested change                                                                                                                                 |
| ---------------------------------------------- | ------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/components/CustomScrollbar/index.tsx:213` | Absolute scrollbar track  | keep with reason | This component is the design-system seam for editor scrollbars; a generic layout primitive cannot express its synchronized overlay geometry and marker layer. | None.                                                                                                                                            |
| `src/components/CustomScrollbar/index.tsx:223` | Clickable marker `<div>`  | fix candidate    | The marker can perform navigation but has no keyboard semantics when it is interactive.                                                                       | Render interactive markers as buttons, or add the appropriate role, focus, and keyboard activation behavior.                                     |
| `src/components/CustomScrollbar/index.tsx:230` | Marker fallback color     | fix candidate    | The literal orange fallback bypasses theme tokens.                                                                                                            | Use the warning color token as the default while preserving caller-provided marker colors.                                                       |
| `src/components/CustomScrollbar/index.tsx:246` | Draggable thumb `<div>`   | fix candidate    | Dragging is pointer-only and the thumb exposes no scrollbar role or keyboard adjustment semantics.                                                            | Add `role="scrollbar"`, value metadata, focus handling, and arrow/page keyboard controls without replacing the underlying native scroll surface. |
| `src/components/CustomScrollbar/index.scss:33` | 30px minimum thumb size   | keep with reason | This is functional drag geometry mirrored by the position calculation, not a decorative spacing value.                                                        | None.                                                                                                                                            |
| `src/components/CustomScrollbar/index.scss:14` | Local z-index 10/11 stack | keep with reason | Track and markers form a self-contained overlay stack inside the editor's isolated positioned container.                                                      | None.                                                                                                                                            |

Verdict totals: **3 fix candidates**, **3 keep with reason**, **0 abstract**.
