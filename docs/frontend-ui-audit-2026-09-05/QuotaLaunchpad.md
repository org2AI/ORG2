# QuotaLaunchpad UI audit

| Line                                               | Element                      | Verdict          | Reason                                                                                                                                                                                                        | Suggested change |
| -------------------------------------------------- | ---------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:148` | View quota quick action      | keep with reason | The action is rendered by the shared `LaunchpadActionCard`, preserving its button semantics, focus style, card/pill responsiveness, and common visual treatment.                                              | None.            |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:396` | Modal mount                  | keep with reason | The start page owns only the short-lived open state; the dedicated modal owns its content and dismissal contract.                                                                                             | None.            |
| `src/engines/ChatPanel/StartPageQuotaModal.tsx:21` | Quota dialog chrome          | keep with reason | Reuses `ModalSystem`, which supplies the application-standard overlay, Escape handling, focus management, and narrow-viewport behavior.                                                                       | None.            |
| `src/engines/ChatPanel/StartPageQuotaModal.tsx:26` | 760px dialog width           | keep with reason | This gives the reused quota grid enough content width for its existing two-card desktop layout; `ModalSystem` caps it at 90vw, so narrow windows retain the responsive one-column fallback.                   | None.            |
| `src/engines/ChatPanel/StartPageQuotaModal.tsx:30` | Quota content                | keep with reason | Reuses `StartPageQuotaGrid`, the Runtime-owned implementation of account cards, empty state, and refresh controls; no duplicate quota presentation is introduced.                                             | None.            |
| `src/engines/ChatPanel/StartPageQuotaGrid.tsx:308` | Quota header/refresh handoff | keep with reason | The Runtime grid keeps the shared Runtime header in its normal placement, while the modal uses the existing `PanelRefreshButton` in the modal action slot; one refresh operation remains the source of truth. | None.            |
| `src/scaffold/ModalSystem/index.tsx:412`           | Modal header actions         | keep with reason | The new generic slot composes icon-only actions with the existing close control, preserving the standard modal chrome and keyboard dismissal behavior.                                                        | None.            |

Verdict totals: **0 fix**, **7 keep with reason**, **0 abstract**.
