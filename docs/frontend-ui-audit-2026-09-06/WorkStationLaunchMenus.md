# WorkStation launch menus UI audit

| Line                                                                         | Element                       | Verdict          | Reason                                                                                                                                                                      | Suggested change |
| ---------------------------------------------------------------------------- | ----------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/WorkStation/AppShell/StartPage/index.tsx:52`                    | Launch action row button      | keep with reason | This is the action surface for the empty WorkStation state and already uses the shared surface hover token; no equivalent menu primitive is needed for this full-width row. | None.            |
| `src/modules/WorkStation/AppShell/StartPage/index.tsx:78`                    | Shortcut hint                 | keep with reason | Reuses the shared `KeyboardShortcut` component and the dropdown variant, with the display value resolved through the platform-aware shortcut registry.                      | None.            |
| `src/modules/WorkStation/AppShell/TabBarPlusMenu/TabBarPlusMenuItems.tsx:32` | Dropdown action button        | keep with reason | Uses the dropdown design-system class and native button semantics for menu actions.                                                                                         | None.            |
| `src/modules/WorkStation/AppShell/TabBarPlusMenu/TabBarPlusMenuItems.tsx:56` | Shortcut hint                 | keep with reason | Reuses the shared `KeyboardShortcut` component and resolves the compact action `shortcutId` through the canonical registry.                                                 | None.            |
| `src/components/KeyboardShortcut/index.tsx:26`                               | Reusable shortcut size prop   | keep with reason | Adds a documented compact option while preserving the existing default size for all current consumers.                                                                      | None.            |
| `src/components/KeyboardShortcut/index.tsx:288`                              | Compact shortcut token styles | keep with reason | Uses the same shared surface/color tokens and scales only dimensions and typography for dense dropdown rows.                                                                | None.            |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.
