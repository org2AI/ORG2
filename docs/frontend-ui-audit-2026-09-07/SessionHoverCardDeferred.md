# SessionHoverCard deferred content UI audit

| Line                                                                     | Element                                      | Verdict          | Reason                                                                                                                                                                                                                                       | Suggested change |
| ------------------------------------------------------------------------ | -------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/components/SessionHoverCard/DeferredSessionHoverCardContent.tsx:53` | Loading and error card shell                 | keep with reason | Reuses HoverCardPanel with its existing 280px width, themed surface, viewport height limit and ResizeObserver-backed portal positioning. The fallback has a token-based minimum content height; resolved content retains its natural height. | None.            |
| `src/components/SessionHoverCard/DeferredSessionHoverCardContent.tsx:54` | Localized fallback text                      | keep with reason | Existing common actions.loading and errors.failedToLoad translations avoid untranslated copy. Loading uses role=status; a chunk failure uses role=alert and does not escape to the app boundary. No raw interactive control is introduced.   | None.            |
| `src/components/SessionHoverCard/index.tsx:22`                           | Deferred content inside existing hover owner | keep with reason | HoverCardBase keeps the existing mouse-delay, leave-delay, trigger event composition, singleton ownership and geometry. Only visible content starts the import. No repeated wrapper framework or new design token is necessary.              | None.            |

Verdict totals: **0 fix**, **3 keep with reason**, **0 abstract**.

All five design-system dimensions were considered for the changed fallback/wrapper; no new hardcoded colors, raw interactive controls or cross-file visual sweep candidates were found. Existing HoverCardBase geometry was not edited.

Verification: focused ESLint passes for index.tsx, DeferredSessionHoverCardContent.tsx, loadSessionHoverCardContent.ts and the three new test files. `pnpm test src/components/SessionHoverCard/SessionHoverCard.test.ts src/components/SessionHoverCard/loadSessionHoverCardContent.test.ts src/components/SessionHoverCard/SessionHoverCard.boundary.test.ts` passes 7 tests across 3 files. Full `pnpm typecheck` and `git diff --check` pass. The boundary regression confirms no static route from the trigger to the details renderer or session overview. Tests use jsdom mouse events and the real HoverCardBase/portal/singleton implementation, with a delayed content fixture. Visual screenshots and actual WebView hover latency were not measured; desktop UI control was not authorized.
