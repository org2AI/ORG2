# Workbench pane controls UI audit

| Line                                 | Element                              | Verdict          | Reason                                                                                                                                                                | Suggested change                                                                                 |
| ------------------------------------ | ------------------------------------ | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `AgentStationTopHeader.tsx:62`       | Agent Station pane-control ownership | fix              | Platform availability was treated as proof that pinned controls were rendered, but populated Agent Station suppresses the pinned group.                               | Gate the header copies on `usePinnedWorkbenchChromeVisible()` so exactly one owner renders them. |
| `useWorkstationTrailingSlot.tsx:100` | My Station pane-control ownership    | fix              | The same availability/visibility mismatch removed maximize and close controls from every populated tab host.                                                          | Use the pinned group's actual visibility and derive one `showPaneControls` invariant.            |
| `useWorkstationTrailingSlot.tsx:207` | Per-host trailing-slot assembly      | fix              | Code and fallback hosts returned identical fragments, while project differed only by one optional action.                                                             | Build the optional project action once and keep one shared return path for all hosts.            |
| `AgentStationTopHeader.tsx:199`      | Pane action buttons                  | keep with reason | Uses the existing `TabBarTrailingIconButton` design-system primitive, which supplies consistent hit area, tooltip delay, shortcut presentation, and button semantics. | None.                                                                                            |
| `useWorkstationTrailingSlot.tsx:130` | My Station pane action buttons       | keep with reason | Uses the same shared chrome button primitive across code, browser, project, and work-item tabs.                                                                       | None.                                                                                            |
| `useWorkstationTrailingSlot.tsx:140` | Compact 14px expand/restore glyphs   | keep with reason | Matches the established pinned-workbench chrome glyph size; the 28px interactive button owns the accessible hit area.                                                 | None.                                                                                            |

Verdict totals: **3 fix**, **3 keep with reason**, **0 abstract**.
