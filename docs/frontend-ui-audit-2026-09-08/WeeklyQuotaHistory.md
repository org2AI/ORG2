# Weekly quota history UI audit — implemented follow-up

| Line                              | Element                           | Verdict          | Reason                                                                                                                                                | Suggested change                                                                                                                          |
| --------------------------------- | --------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `WeeklyQuotaHistoryPanel.tsx:31`  | Account chart stack               | fix              | Original rendering mounted 128 charts for 128 accounts, pushing the rest of Runtime Usage far below the fold                                          | Implemented one selected account chart with the shared searchable Select; existing quota summary remains above this section               |
| `WeeklyQuotaHistoryPanel.tsx:51`  | Error versus empty-account state  | fix              | Failed local history reads also instructed users to connect an account, even when account existence was unknown                                       | Implemented mutually exclusive error/empty states and read-only reload control                                                            |
| `WeeklyQuotaHistoryPanel.tsx:112` | Old and disabled observations     | fix              | Historical readings looked current; disabled accounts could appear to be awaiting an automatic sample                                                 | Implemented timestamp, stale/disabled copy, no-observation fallback and explicit display cap; the backend owns the disabled sampling gate |
| `WeeklyQuotaHistoryPanel.tsx:140` | Chart accessibility / time domain | fix              | An outer img role hid the interactive chart's accessibility semantics; a single-point dataMin/dataMax axis had no useful range                        | Implemented group semantics and a stable 28-day domain from the hook's read timestamp; observation dots retain isolated samples           |
| `WeeklyQuotaHistoryPanel.tsx:38`  | Header and reload action          | keep with reason | Reuses RuntimeSectionHeader and RuntimeRefreshButton; reload reads SQLite and cannot bypass provider cooldown                                         | None                                                                                                                                      |
| `WeeklyQuotaHistoryPanel.tsx:69`  | Account selection                 | keep with reason | Uses the existing Select with an accessible label and search; no custom button/keyboard/dropdown behavior                                             | None                                                                                                                                      |
| `WeeklyQuotaHistoryPanel.tsx:145` | Chart styling                     | keep with reason | Existing Recharts/chart tokens, semantic color, 0–100 axis and disabled animation; chart gaps reflect missing observations rather than invented usage | None                                                                                                                                      |

Verdict totals: **4 fix (implemented)**, **3 keep with reason**, **0 abstract**.

The 128-account mount-count and contradictory error/empty-state tests both failed before the UI change and pass afterward. Component tests mock chart internals: the measured claim is 128 → 1 chart mounts, not a measured WebView speedup. Source review covers light/dark semantic tokens and narrow layout; actual desktop screenshots and assistive-technology checks were not run because computer control remains explicitly opt-in.
