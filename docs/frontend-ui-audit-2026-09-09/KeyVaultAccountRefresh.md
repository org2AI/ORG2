# Key Vault account refresh UI audit

| Line                                                                                      | Element                         | Verdict          | Reason                                                                                                                                                                                    | Suggested change |
| ----------------------------------------------------------------------------------------- | ------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/MainApp/Integrations/KeyVault/Accounts/Table/AccountInlineActionsBar.tsx:51` | Usage and model refresh actions | keep with reason | Both actions continue to use the shared `Button` primitive and existing localized labels; the change only makes their disabled state mutually exclusive while either operation is active. | None.            |
| `src/modules/MainApp/Integrations/KeyVault/Accounts/Table/MyAccountsTableSection.tsx:338` | Per-account loading projection  | keep with reason | Keyed sets preserve independent loading indicators for concurrently refreshed accounts without adding a visual pattern or duplicating button chrome.                                      | None.            |
| `src/modules/MainApp/Integrations/KeyVault/Table/AccountsTable.tsx:582`                   | Refresh-state prop plumbing     | keep with reason | Explicit usage/model sets keep operation semantics visible across the existing table boundary; two related props are below the three-site abstraction threshold.                          | None.            |
| `src/modules/MainApp/Integrations/KeyVault/Accounts/Table/AccountInlineActionsBar.tsx:76` | Disabled interaction semantics  | keep with reason | Native shared buttons retain keyboard behavior, accessible labels, and disabled semantics. No raw interactive element, arbitrary Tailwind value, hardcoded color, or size was introduced. | None.            |

Verdict totals: **0 fix**, **4 keep with reason**, **0 abstract**.
