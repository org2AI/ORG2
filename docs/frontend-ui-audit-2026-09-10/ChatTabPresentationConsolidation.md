# Chat tab presentation consolidation UI audit

| Line                                                              | Element                           | Verdict          | Reason                                                                                                                                                                    | Suggested change                                                                           |
| ----------------------------------------------------------------- | --------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `src/engines/ChatPanel/ChatPanelTabBar/ChatPanelTabIcon.tsx:37`   | Expanded/collapsed icon selection | fix              | Both surfaces now use one entity renderer with shared SVG attributes. Session identity, private-channel icons, and integration icons retain their specialized components. | Implemented; preserve the heading's explicit existing visibility policy.                   |
| `src/components/TabPill/TabPillCloseButton.tsx:42`                | Close-control appearance          | fix              | One primitive now owns duplicated base styles and visibility classes across both production callers.                                                                      | Implemented as a single two-consumer sweep; callers still own placement and unsaved state. |
| `src/engines/ChatPanel/ChatPanelHeader.tsx:343`                   | Trailing controls                 | fix              | Expanded and collapsed rows now call the same renderer.                                                                                                                   | Implemented; retain h-9/h-7 and collapsed test marker.                                     |
| `src/engines/ChatPanel/ChatPanelTabBar/ChatPanelPlusMenu.tsx:166` | Menu items                        | keep with reason | Shared dropdown tokens and existing keyboard-shortcut/recent-tab components already define appearance. Only duplicate action-prop declarations were consolidated.         | No new menu framework.                                                                     |
| `src/engines/ChatPanel/ChatPanelTabBar/TabPill.tsx:86`            | Tab surface                       | keep with reason | Shared surface, scrim and close button remain; drag handlers and hover-card ownership are unchanged.                                                                      | Preserve entity adapters.                                                                  |
| `src/engines/ChatPanel/ChatPanelTabBar/index.tsx:295`             | Pair separators                   | keep with reason | The shared separator token already aligns the strips; extracting a new wrapper is optional and not needed for this presentation-ownership change.                         | Keep the existing markup in this PR.                                                       |

Verdict totals: **3 fix**, **3 keep with reason**, **0 abstract**.

Validation: expanded/collapsed icon parity tests, existing Chat and Workstation rendering tests, typecheck and lint. No live screenshots, theme or viewport verification: desktop control remains explicit opt-in. Layout classes and existing heading icon omissions are preserved. This is the implementation audit, not the earlier audit-only report.
