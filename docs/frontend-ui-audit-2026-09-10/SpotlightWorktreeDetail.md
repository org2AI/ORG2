# Spotlight worktree and workspace detail UI audit

Branch/ref rows do not show detail cards. Worktree cards retain their secondary branch metadata.

| Line                                                                  | Element                        | Verdict          | Reason                                                                                                                                                                                                                                                                                                                       | Suggested change |
| --------------------------------------------------------------------- | ------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/scaffold/GlobalSpotlight/components/SpotlightDetailPane.tsx:113` | Workspace repository titles    | keep with reason | Titles are icon-free; folder icons accompany secondary paths through the shared DetailLine component                                                                                                                                                                                                                         | None             |
| `src/scaffold/GlobalSpotlight/components/SpotlightDetailPane.tsx:124` | Repository and worktree titles | keep with reason | Plain first line follows the requested shared presentation; branch and path metadata use equally styled secondary rows                                                                                                                                                                                                       | None             |
| `src/scaffold/GlobalSpotlight/components/SpotlightDetailPane.tsx:23`  | DetailLine path button         | keep with reason | Shared row uses 14px icons consistent with Settings table cells. Native button provides keyboard activation, focus ring and underline; open-folder icon appears on hover/focus. Absolute local paths use the desktop opener, and clicks stop row selection. Styling follows the Settings credential-import path link pattern | None             |

Verdict totals: **0 fix**, **3 keep with reason**, **0 abstract**.

D1–D5 checked: existing hover-card scaffold and icon primitives reused, theme colors retained, all path interaction is in one shared row. Path buttons are named by visible text; native title tooltips and redundant card aria labels are omitted as requested. The hover icon sits immediately after the path. Visual desktop verification and actual OS opening were not run because computer control was not requested; opening is verified with a mocked desktop opener.
