# ActivityGroupProjection UI audit

Scope: the new shared render projection `src/engines/ChatPanel/ChatItems/activityGroupProjection.tsx` and the three consumers that now delegate to it (`EditActivityGroup`, `TerminalActivityGroup`, `ActionSummaryGroup`). Behavior-preserving refactor; each consumer's diff is the removal of its private `ActivityBlock` / `suppressLoadingForNonLastRunningEvent` / `render*Event` copy plus the import swap. No rendered classes, copy, layout, or interaction contract changed.

| Line                                                                  | Element                                                           | Verdict          | Reason                                                                                                                                                                                                                                                                                       | Suggested change                                                                                               |
| --------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `src/engines/ChatPanel/ChatItems/activityGroupProjection.tsx:74`      | `Suspense` + `ChatLoadingBlock` + tail-only running-state wrapper | abstract         | D5: the identical triple existed in three groups (edit, terminal, explore). This PR lands the seam; the three sites now share one owner so the loading-state contract cannot drift.                                                                                                          | Done in this PR: `renderActivityGroupEvent` / `buildActivityGroupItems` / `markActivityGroupTail`.             |
| `src/engines/ChatPanel/ChatItems/activityGroupProjection.tsx:17`      | `ChatLoadingBlock` fallback                                       | keep with reason | D1: the fallback is the design-system loading primitive from `blocks/primitives`; no raw markup introduced.                                                                                                                                                                                  | None.                                                                                                          |
| `src/engines/ChatPanel/ChatItems/EditActivityGroup/index.tsx:150`     | `StackedBlock` `renderItem={renderActivityGroupEvent}`            | keep with reason | D1/D5: `StackedBlock` is the DS stack primitive; the edit/read count and `DIFF_STATS` diff-stat summary (`:115-140`) is edit-domain and stays local. Icon size/`text-text-2` are unchanged pre-existing values, not introduced here.                                                         | None.                                                                                                          |
| `src/engines/ChatPanel/ChatItems/TerminalActivityGroup/index.tsx:158` | `StackedBlock` `renderItem={renderActivityGroupEvent}`            | keep with reason | D1/D5: same DS stack primitive; command/MCP/wait/inspect summary and the durable `OrgtrackEnvelopeCard` Work Item projection are terminal-domain and stay local.                                                                                                                             | None.                                                                                                          |
| `src/engines/ChatPanel/ChatItems/ActionSummaryGroup/index.tsx:144`    | `StackedBlock` `renderItem={renderActivityGroupEvent}`            | keep with reason | D1/D5: same DS stack primitive; explore items carry a `category`, so the tail tag is applied via the generic `markActivityGroupTail` (`:107`) instead of the event-list helper. The former `isLastItem?: boolean` + `=== true` guard is replaced by a required boolean with the same result. | None.                                                                                                          |
| `src/engines/ChatPanel/ChatItems/ActionSummaryGroup/index.tsx:132`    | `HugeiconsIcon size={14} className="text-text-2"`                 | keep with reason | D3: pre-existing literal icon size shared with the other two groups via `getToolIcon(..., { size: 14 })`; not touched by this refactor and consistent across the three groups. `WorkActivityGroup` already uses `SESSION_UI_TOKENS.ICON.SIZE_SM` for the same value.                         | Out of scope here; a follow-up could switch the three groups to `SESSION_UI_TOKENS.ICON.SIZE_SM` in one sweep. |
| `src/engines/ChatPanel/ChatItems/WorkActivityGroup/index.tsx:35`      | Paged `Suspense` + `ChatLoadingBlock` per event                   | watch            | Same fallback pair, but keyed per event inside a page and with no tail-only running suppression, so it is not the same projection. Confirm whether the paged group should also suppress stale running states before folding it into the shared module.                                       | None now; revisit if the paged group grows a running-state rule.                                               |

Verdict totals: **0 fix**, **5 keep with reason**, **1 abstract**.

Sweep candidates: 0 new (the `size={14}` icon literal is noted above as a possible token follow-up, not a finding of this change). Accessibility: no semantic or interactive change; `StackedBlock` retains its existing header/collapse contract. No screenshots: the diff is a pure extraction with identical element output, verified by the existing `TerminalActivityGroup` / `WorkActivityGroup` render tests plus the new `activityGroupProjection` test.
