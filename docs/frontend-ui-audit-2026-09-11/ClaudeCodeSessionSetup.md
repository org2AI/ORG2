# ClaudeCodeSessionSetup UI audit

Scope: `src/features/SessionSetup/components/ClaudeCodeSessionSetup/index.tsx` after the shared `OAuthSessionSetupShell` extraction. The component now owns only its capture hook, provider metadata mapping, WebView position sync and debug rows.

| Line                                   | Element                              | Verdict          | Reason                                                                                                                                                                                                                                    | Suggested change |
| -------------------------------------- | ------------------------------------ | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `ClaudeCodeSessionSetup/index.tsx:64`  | Capture and account metadata mapping | keep with reason | Claude Code owns its callback response and optional organization / rate-limit metadata (`toClaudeCodeAccountMetadata`); generalizing this into the shell would weaken the provider types. Covered by `ClaudeCodeSessionSetup.test.ts`.    | None.            |
| `ClaudeCodeSessionSetup/index.tsx:77`  | `useWebviewPositionSync` call        | keep with reason | Stays in the provider next to the hook that returns `updatePosition`; moving it into the shell would relocate the pre-existing `no-misused-promises` finding to a new file and trip the typed-lint ratchet for no design gain.            | None.            |
| `ClaudeCodeSessionSetup/index.tsx:79`  | `hasToken` derivation                | keep with reason | Needs `accessToken`, a provider-only field of the capture hook; the shell takes the boolean.                                                                                                                                              | None.            |
| `ClaudeCodeSessionSetup/index.tsx:82`  | `OAuthSessionSetupShell` usage       | keep with reason | `providerId="claude-code"` reproduces every `data-testid` the component rendered before (`claude-code-session-setup`, `-oauth-signin`, `-oauth-browser-shell`, `-oauth-current-url`, `-oauth-browser-close`, `-oauth-webview-container`). | None.            |
| `ClaudeCodeSessionSetup/index.tsx:91`  | `copy` map of `t()` calls            | keep with reason | Each key maps 1:1 onto a `t()` call that was inline before; no new i18n keys, so `check:i18n-keys` sees no change.                                                                                                                        | None.            |
| `ClaudeCodeSessionSetup/index.tsx:106` | Debug rows                           | keep with reason | Provider-specific token rows rendered inside the shell's shared debug container.                                                                                                                                                          | None.            |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.
