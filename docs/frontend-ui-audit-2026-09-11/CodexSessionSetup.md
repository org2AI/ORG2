# CodexSessionSetup UI audit

Scope: `src/features/SessionSetup/components/CodexSessionSetup/index.tsx` after the shared `OAuthSessionSetupShell` extraction. The component now owns only its capture hook, required-token mapping, `autoStart` input, WebView position sync and debug rows.

| Line                             | Element                            | Verdict          | Reason                                                                                                                                                                                                                                     | Suggested change |
| -------------------------------- | ---------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| `CodexSessionSetup/index.tsx:46` | Capture and required token mapping | keep with reason | Codex requires refresh and ID tokens; that is a provider contract difference, not a presentation variant, so the mapping stays in the provider. Covered by `CodexSessionSetup.test.ts`.                                                    | None.            |
| `CodexSessionSetup/index.tsx:61` | `useWebviewPositionSync` call      | keep with reason | Same reasoning as Claude Code: stays beside the hook that returns `updatePosition`, and keeps the pre-existing typed-lint finding in place.                                                                                                | None.            |
| `CodexSessionSetup/index.tsx:63` | `hasToken` derivation              | keep with reason | Needs `accessToken`, a provider-only field of the capture hook; the shell takes the boolean.                                                                                                                                               | None.            |
| `CodexSessionSetup/index.tsx:66` | `OAuthSessionSetupShell` usage     | keep with reason | `providerId="codex"` reproduces every previous `data-testid`; `initiallyOpen={autoStart}` is the only prop Claude Code does not pass and maps onto the `useState(autoStart)` the component had. Covered by the `autoStart` lifecycle test. | None.            |
| `CodexSessionSetup/index.tsx:76` | `copy` map of `t()` calls          | keep with reason | Each key maps 1:1 onto a `t()` call that was inline before; no new i18n keys.                                                                                                                                                              | None.            |
| `CodexSessionSetup/index.tsx:91` | Debug rows including `Id Token`    | keep with reason | Provider-specific rows rendered inside the shell's shared debug container; the extra `Id Token` row needs no shell flag.                                                                                                                   | None.            |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.
