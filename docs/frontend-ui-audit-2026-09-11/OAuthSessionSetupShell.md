# OAuthSessionSetupShell UI audit

Scope: `src/features/SessionSetup/components/OAuthSessionSetupShell.tsx`, the shared presentation and browser lifecycle extracted from `ClaudeCodeSessionSetup` and `CodexSessionSetup` (re-landing of #780 commit `ba4fbc1a23` on current develop, behavior-preserving only). Provider components are audited in their own files in this folder.

| Line                                                    | Element                                                 | Verdict          | Reason                                                                                                                                                                                                                                                                                                                       | Suggested change                                                                                                                                                    |
| ------------------------------------------------------- | ------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OAuthSessionSetupShell.tsx:98`                         | Shared idle / browser / loading / error / success shell | abstract         | D5. The identical 300-line shell (section row, WebView chrome, step indicator, three overlays, two notices, debug container) was rebuilt in `ClaudeCodeSessionSetup` and `CodexSessionSetup`, and `CursorSessionSetup` carries the same header, spinner and error-overlay chrome: 3 occurrences, threshold met.              | Landed by this PR for the two PKCE providers. Cursor is deliberately excluded; see `CursorSessionSetup.md` for the lifecycle differences that keep it out.          |
| `OAuthSessionSetupShell.tsx:135`                        | Browser auto-start                                      | keep with reason | Delegates to `useOAuthBrowserAutoStart` (develop c6bf7e423) which fires one PKCE attempt per browser open. The `showBrowser && !isWebviewOpen && !isSigningIn -> setTimeout(startLogin, 100)` effect from the original #780 shell is not carried over: on macOS the native webview can close while `showBrowser` stays true. | None.                                                                                                                                                               |
| `OAuthSessionSetupShell.tsx:137`, `:147`                | `closeWebview()` / `startLogin()` promise handling      | keep with reason | `.catch(() => undefined)` replaces the providers' `void` discard so the moved lines do not register as new `no-floating-promises` findings under the typed-lint ratchet. Both hook functions catch internally and never reject, so there is no behavior change.                                                              | None.                                                                                                                                                               |
| `OAuthSessionSetupShell.tsx:162`                        | Idle sign-in row                                        | keep with reason | Uses the design-system `SectionContainer` / `SectionRow` / `Button` primitives exactly as both providers did.                                                                                                                                                                                                                | None.                                                                                                                                                               |
| `OAuthSessionSetupShell.tsx:177`                        | `className="h-8 min-h-8"` on the sign-in `Button`       | keep with reason | Height override carried verbatim from both providers; it appears in 10 KeyVault setup files (Cursor, Copilot, Kiro, Generic, ApiKeyProvider, LocalModel, ClaudeCodeSetup, CodexSetup and this shell), so it is one KeyVault-wide row-height decision, not a per-site fix.                                                    | Sweep candidate (1): give the KeyVault setup row a named Button size or a `SectionRow` action slot height instead of ten `h-8 min-h-8` literals. Out of scope here. |
| `OAuthSessionSetupShell.tsx:201`, `:213`                | Refresh / close icon-only buttons                       | keep with reason | D4. Icon-only `Button`s without `aria-label` / `title`, exactly as on develop. Adding accessible names is an additive behavior change and ships in the stacked follow-up `fix(auth): coalesce oauth retries and make close idempotent`, not in this behavior-preserving extraction.                                          | Follow-up PR adds `aria-label` + `title` from `common:actions.retry` / `common:actions.close`.                                                                      |
| `OAuthSessionSetupShell.tsx:223`, `:235`                | Step indicator                                          | keep with reason | Reuses `SessionSetupStepIndicator` (develop b91cb0474) instead of the private `StepIndicator` copy the original #780 shell re-inlined.                                                                                                                                                                                       | Follow-up PR exposes the active step with `aria-current="step"` inside `SessionSetupStepIndicator`.                                                                 |
| `OAuthSessionSetupShell.tsx:254`, `:265`                | Loading and error overlays                              | keep with reason | D4. Overlays carry no `role="status"` / `role="alert"`, matching develop. Same reasoning as the icon buttons: semantics land in the stacked follow-up.                                                                                                                                                                       | Follow-up PR adds `role="status"` (loading) and `role="alert"` (error).                                                                                             |
| `OAuthSessionSetupShell.tsx:191`, `:243`, `:273`-`:304` | `text-[12px]` / `text-[14px]` typography literals       | keep with reason | D2/D3. Carried verbatim from both providers; `text-[12px]` is used in 199 `.tsx` files repo-wide and previous audits in this series treat this metadata scale as keep-with-reason because no smaller typography token exists. Changing them would alter rendering in a refactor that must not.                               | None.                                                                                                                                                               |
| `OAuthSessionSetupShell.tsx:314`, `:318`                | Success and error notices                               | keep with reason | Uses `PageNotice` (the `InlineAlert` the original #780 shell imported was removed on develop). `onClose` routes capture errors to `reset` and token errors to `onClearTokenError`, exactly as each provider did.                                                                                                             | None.                                                                                                                                                               |
| `OAuthSessionSetupShell.tsx:327`                        | Debug container                                         | keep with reason | Shared wrapper with the same `bg-bg-3` / `text-[11px]` treatment; provider-specific rows come in through `debugContent`, so Codex keeps its `Id Token` row without a shell flag.                                                                                                                                             | None.                                                                                                                                                               |

Verdict totals: **0 fix**, **9 keep with reason**, **1 abstract**.

Sweep candidates: 1 (`h-8 min-h-8` setup-row height, 10 files under `src/features/SessionSetup` and `src/scaffold/WizardSystem/variants/KeyVault`).

No theme/viewport screenshots: the extraction reproduces both providers' markup, class lists and `data-testid`s byte-for-byte for `claude-code` and `codex`, verified by rendered jsdom lifecycle tests (`OAuthSessionSetupShell.test.ts`, `ClaudeCodeSessionSetup.test.ts`, `CodexSessionSetup.test.ts`). Live native OAuth on macOS was not exercised in this session.
