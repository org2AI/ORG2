# PR850 integration UI audit

Scope: conflict resolutions and browser adapters after integrating September 11 develop. The earlier feature reports cover the original viewer surfaces.

| Line                                                                                                          | Element                              | Verdict          | Reason                                                                                                                        | Suggested change                                                          |
| ------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ---------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `src/engines/ChatPanel/header/ChatPanelChrome.tsx:95`                                                         | Published header and pinned controls | fix              | The extracted frame must retain develop's right-edge reservation, current surface token, and inset transition                 | Applied; pass the desktop reservation into the shared frame               |
| `src/scaffold/NavigationSidebar/SidebarBase.tsx:338`                                                          | Native sidebar chrome                | fix              | Browser hosts explicitly disable native chrome and request an opaque surface; the current base had dropped those capabilities | Applied; preserve desktop defaults and honor the browser props            |
| `src/web/features/auth/WebLoginPage.tsx:32`                                                                   | Login shell                          | fix              | The old onboarding layout was removed on develop                                                                              | Applied; use the current shared LoginCard                                 |
| `src/web/shell/WebSessionSidebar.tsx:157`                                                                     | Session search                       | fix              | The old sidebar search component and config were removed; browser search remains a feature requirement                        | Applied; use SearchInput and keep tree filtering owned by the web adapter |
| `src/scaffold/NavigationSidebar/connectors/WorkstationSidebarConnector/cloudSessionsSection.menuItems.tsx:88` | Session filter button                | keep with reason | Retains develop's hover visibility and the existing optional browser filter capability                                        | None                                                                      |
| `src/engines/Simulator/components/SimulatorStatusBar/SimulatorStatusBarView.tsx:102`                          | Free-browse control                  | keep with reason | The shared controlled view preserves the current accessible label and desktop test identifier                                 | None                                                                      |

Verdict totals: **4 fix**, **2 keep with reason**, **0 abstract**.

Verification uses static rendering and unit tests. No desktop UI automation was used; the user's explicit opt-in preference excludes it. Authenticated hosted screenshots and responsive visual checks remain unverified.
