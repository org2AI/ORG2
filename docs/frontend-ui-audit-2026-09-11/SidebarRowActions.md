# Shared sidebar and header buttons UI audit

| Line                                                                   | Element                                     | Verdict          | Reason                                                                                                                                                                      | Suggested change                                                                                                                                                                                      |
| ---------------------------------------------------------------------- | ------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/Button/presentation.tsx:25`                            | Shared compact button sizing and appearance | fix              | User-authorized sweep replaces IconButton and local row button implementations with shared Button props.                                                                    | Added size="sidebar" (20px), retained standard 8px radius, and added appearance="soft" with neutral default / light danger-1 hover. Removed IconButton and migrated all 14 production consumer files. |
| `src/components/TreeRow/TreeRowAction.tsx:40`                          | Tree action wrapper                         | keep with reason | Now renders Button with the shared props; wrapper retains domain icon data, tooltip, and parent-row reveal behavior.                                                        | None.                                                                                                                                                                                                 |
| `src/modules/shared/layouts/blocks/WorkstationTrailSurface.tsx:138`    | Workstation trail action wrapper            | keep with reason | Delegates rendering and styling to Button. Defaults to sidebar size; collapsed controls explicitly retain small (28px). Removed the separate trail icon-button class token. | None.                                                                                                                                                                                                 |
| `src/modules/WorkStation/shared/TerminalNewSessionSplitButton.tsx:135` | Terminal controls                           | keep with reason | Standalone sidebar creation action uses Button. Joined controls continue using shared SPLIT_BUTTON geometry, preserving the narrow picker segment.                          | None.                                                                                                                                                                                                 |
| `src/modules/MobileRemote/components/MobileTopBar.tsx:26`              | Mobile navigation                           | keep with reason | Migrated to Button; explicit mobile-touch-size dimensions preserve the existing CSS touch target despite Button inline dimensions. Mobile stop control does the same.       | None.                                                                                                                                                                                                 |
| `src/config/workstation/tokens.ts:82`                                  | Compact action palette                      | keep with reason | One neutral/default and light danger palette is shared by Button soft appearance and existing header tokens; no per-domain palette is introduced.                           | None.                                                                                                                                                                                                 |

Verdict totals: **1 fix**, **5 keep with reason**, **0 abstract**.

Architecture coverage: layers 1–7 checked for types, live callers, removed imports, naming, variant defaults, ownership, and discoverability. Layers 8–10 do not apply: no serialization, initialization, or resolver changes. No background work or retained-state behavior changed.

Verification: targeted ESLint with zero warnings and TypeScript checks passed during migration. Button tests cover rendered sidebar dimensions/radius, regular and danger styling, icon rendering, and native disabled click behavior. Existing navigation, canvas, mobile, PR-header, and trail suites validate migrated callers. Publication verification on latest develop: all 61 targeted tests across 10 suites passed; full typecheck, changed-file ESLint, and diff whitespace checks passed. Desktop visual verification was not run because computer control was not requested.

Non-sidebar former 20px IconButton callers use the existing mini (24px) Button size. Explicit 28px controls remain small; compact sidebar and trail controls use sidebar (20px). The selected radius for all migrated standard controls is 8px.
