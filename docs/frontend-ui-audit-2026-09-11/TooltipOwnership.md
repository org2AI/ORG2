# Tooltip ownership UI audit

Scope: duplicate visible labels on controls with existing tooltips. Searched `src/**/*.tsx` for tooltip/title combinations, including JSX ancestry and shared controls returned through variables. Reviewed changed controls across D1–D5; no new token, color, or size changes were needed.

| Line                                                                                                                                                                                                                                                                            | Element                            | Verdict          | Reason                                                                                                                                                                                                                                                         | Suggested change                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/TabPill/TabBarTrailingIconButton.tsx:50`; `src/modules/MainApp/Settings/components/SettingsHeaderActions.tsx:45`; `src/modules/shared/components/FileHeader/FileHeaderMoreMenu.tsx:257`; `src/engines/ChatPanel/InputArea/components/CollapsedInlineRow.tsx:94` | Single tooltip owner sweep (D4/D5) | fix              | Shared tab-bar controls emitted native titles alongside styled tooltips; Settings and file-header controls nested styled tooltips; the add-to-conversation wrapper also covered the separately named cancel action. These are one duplicate-hover-label sweep. | Applied: native titles only when the styled tooltip is disabled and native fallback is allowed; disable inner tooltips when an outer tooltip owns the control; scope the add tooltip to its button. |
| `src/components/TabPill/TabBarTrailingIconButton.tsx:51`; `src/modules/MainApp/Settings/components/SettingsHeaderActions.tsx:48`; `src/engines/ChatPanel/InputArea/components/CollapsedInlineRow.tsx:140`                                                                       | Accessible button names (D4)       | keep with reason | `aria-label` supplies screen-reader names and does not produce visible browser hover text. Removing it would leave icon buttons without reliable accessible names.                                                                                             | Retain accessible names.                                                                                                                                                                            |
| `src/engines/ChatPanel/InputArea/components/CollapsedInlineRow.tsx:141`; `src/components/TabPill/TabBarTrailingIconButton.tsx:50`                                                                                                                                               | Native-only tooltips (D1/D4)       | keep with reason | Cancel now sits outside the add tooltip. Native fallback remains useful when no styled tooltip owns the control; existing opt-outs remain respected.                                                                                                           | Retain standalone native titles.                                                                                                                                                                    |

Verdict totals: **1 fix**, **2 keep with reason**, **0 abstract**.

Verification: `pnpm test src/components/TabPill/TabBarTrailingIconButton.test.ts src/modules/shared/components/FileHeader/FileHeaderMoreMenu.test.ts src/engines/ChatPanel/InputArea/components/CollapsedInlineRow.test.ts` — 11 tests passed. The new rendered hover regression verifies one styled tooltip, absence of native title, retained accessible name, and disabled-tooltip fallback/opt-out behavior. No desktop UI control or visual capture was performed, per user preference.

Additional checks: `pnpm typecheck:fast` passed; targeted ESLint on the four changed components and the new test passed; Prettier formatting and `git diff --check` passed. The four new hover/fallback tests were rerun successfully after adjusting the test renderer to satisfy lint.
