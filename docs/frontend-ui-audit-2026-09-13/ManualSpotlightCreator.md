# ManualSpotlightCreator UI audit

Scope: manual Work Item and Project creation; comparison with session creation, commit, quota, repository creation, and shared-session import Spotlight forms. User requested implementing the header and content-spacing sweep.

| Line                                                                          | Element                                          | Verdict          | Reason                                                                                                                   | Suggested change                                                                                                            |
| ----------------------------------------------------------------------------- | ------------------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| `src/modules/ProjectManager/shared/components/ManualSpotlightCreator.tsx:51`  | Shared Work Item / Project modal header and body | fix              | Both creation flows used a plain heading and independent bottom spacing instead of the existing Spotlight form structure | Applied `SpotlightFormLayout` with the action breadcrumb/back control and `SpotlightFormBody` for canonical content padding |
| `src/modules/ProjectManager/shared/components/CreateComposerScaffold.tsx:117` | Spotlight composer outer gutter                  | fix              | Chat-panel padding stacked with the modal body inset                                                                     | Applied chat gutters only in page mode; Spotlight body owns the inset                                                       |
| `src/scaffold/GlobalSpotlight/forms/shared/SpotlightFormShell.tsx:48`         | Shared body spacing                              | keep with reason | Canonical p-3 inset aligns form sections; now owned by `SPOTLIGHT_MODAL_FORM_TOKENS.bodyClassName`                       | Retain the existing spacing value                                                                                           |
| `src/modules/ProjectManager/shared/components/CreateComposerScaffold.tsx:131` | Editor surface and controls                      | keep with reason | Shared ComposerSurface owns editor chrome and internal control spacing for both creator modes                            | Retain existing editor structure                                                                                            |
| `src/scaffold/GlobalSpotlight/palettes/SessionCreatorPalette/index.tsx:54`    | Reference modal structure                        | keep with reason | Already uses SpotlightFormLayout and SpotlightFormBody, with inner padding removed                                       | None                                                                                                                        |
| `src/modules/ProjectManager/shared/components/CreateComposerScaffold.tsx:66`  | Creator property dropdown direction              | fix              | Upward placement from bottom-docked pages clipped Spotlight controls above the modal                                     | Work Item and Project Spotlight callers select downward placement; Project organization select also uses bottom placement   |

Verdict totals: **3 fix**, **3 keep with reason**, **0 abstract**.

No new background work, subscriptions, or retained state. GUI verification was not performed because computer control was not authorized; verification uses rendered component tests and static checks.

Dropdown follow-up verification: 17 tests passed across CreateComposerScaffold, PropertyDropdownField, and ProjectOrganizationSelect; `pnpm typecheck:fast` and scoped ESLint passed. Desktop visual verification remains unperformed.
