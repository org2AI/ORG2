# Shared search fields UI audit

This completes the six-site search-field migration identified in `RemainingNativeControls.md`.

| Line                                                                                                                                                                                                                                                                   | Element                                          | Verdict          | Reason                                                                                                                                                                                                        | Suggested change                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| [SearchInput:255](../../src/components/SearchInput/index.tsx#L255), [ReplaceInput:154](../../src/modules/WorkStation/CodeEditor/Panels/shared/ReplaceInput.tsx#L154), [SearchFilters:83](../../src/modules/WorkStation/CodeEditor/Panels/shared/SearchFilters.tsx#L83) | Four single-line fields and two multiline fields | fix              | All six now use shared Input or Textarea with bare appearance. Shared callbacks are adapted to preserve the public string-only callback contract; refs and native label IDs still reach the editable element. | Completed as one authorized sweep.            |
| Same three components                                                                                                                                                                                                                                                  | Search shell styling                             | keep with reason | The shell already owns surface, focus treatment, icons, and action placement. Bare shared controls, auto-height Input, and removal of its inner border avoid duplicating shell spacing and focus chrome.      | Keep surrounding panel/sidebar/ghost formats. |
| [searchControlInputStyles.ts](../../src/components/SearchInput/searchControlInputStyles.ts)                                                                                                                                                                            | Text geometry and multiline cap                  | keep with reason | Search text retains its 28px single-line height and existing first-line alignment. Explicit minimum height zero overrides Textarea's general-purpose 60px minimum; the existing 120px resize cap remains.     | Keep domain geometry and resize handlers.     |

D1: no raw input or textarea JSX remains in the three migrated components. D2/D3: existing row/font dimensions are retained through element-style props; no new primitive API or global token change. D4: label associations, refs, Enter submission, Shift+Enter, and change callbacks are covered by rendered tests. Shared fields now own the select-all shortcut instead of SearchInput registering it twice. D5: reuse existing Input/Textarea rather than adding another field abstraction.

Verdict totals: **1 fix**, **2 keep with reason**, **0 abstract**.

Verification: focused shared-input/search/textarea tests, changed-file ESLint, TypeScript check, and diff whitespace check. Desktop visual verification was not run because computer control is explicit opt-in; nested wrapper rendering across desktop platforms remains unverified.
