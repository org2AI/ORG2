# HeaderActionGroup UI audit

| Line                                                                                                                                                                                   | Element                             | Verdict          | Reason                                                                                                                                                    | Suggested change                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/WindowChrome/HeaderActionGroup.tsx:6`, `src/modules/WorkStation/shared/TabBar/components/TabBarControls/index.tsx:37`, `src/engines/ChatPanel/ChatPanelHeader.tsx:348` | Station and chat control containers | fix              | User requested one stable container with 36px height and 1px gaps across both panes                                                                       | Applied shared HeaderActionGroup, including collapsed chat chrome; removed redundant workstation padding branches and drag wrapper |
| `src/modules/WorkStation/shared/TabBar/index.tsx:408`, `src/engines/ChatPanel/ChatPanelHeader.tsx:495`                                                                                 | Two-column tab rows                 | keep with reason | minmax(0,1fr) lets tab content shrink while the auto column reserves the controls' intrinsic width; this is layout geometry, not an arbitrary visual size | None                                                                                                                               |
| `src/components/WindowChrome/HeaderActionGroup.tsx:12`                                                                                                                                 | Non-draggable group                 | keep with reason | Reuses NoDragRegion; existing controls remain shared Button-based components with labels, handlers and keyboard semantics preserved                       | None                                                                                                                               |
| `src/engines/ChatPanel/header/ChatPanelPublishedHeader.tsx:41`                                                                                                                         | Published-header edge inset         | keep with reason | Retains the documented 7px chat inset to account for the pane separator; removes padding animation without changing the reserved space                    | None                                                                                                                               |

Verdict totals: **1 fix**, **3 keep with reason**, **0 abstract**.

Scope: requested My Station and chat-pane bars. AST inspection of all six changed production files found no native button JSX, native button creation or substitute clickable elements. No new input controls, colors, subscriptions or timers were introduced.

Verification: 31 targeted Vitest tests passed across ChatPanelHeader, ChatPanelPublishedHeader, useWorkstationTrailingSlot.visibility and SortableTab. `pnpm run typecheck:fast`, targeted ESLint and `git diff --check` passed. Live visual verification was not performed because desktop computer control was not authorized; unit tests do not establish rendered motion behavior.
