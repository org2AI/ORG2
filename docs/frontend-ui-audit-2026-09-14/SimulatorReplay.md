# Simulator replay UI audit

| Line                                                                    | Element                   | Verdict          | Reason                                                                                                                                                                      | Suggested change                                                                |
| ----------------------------------------------------------------------- | ------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| `src/engines/Simulator/ActivitySimulatorGrid.tsx:94`                    | Session-scoped cell mount | keep with reason | Identity changes reuse the existing grid and cell components; no duplicate visual scaffold.                                                                                 | None.                                                                           |
| `src/engines/Simulator/components/GridCell/IndependentGridCell.tsx:263` | Replay action controls    | keep with reason | Existing shared Button controls retain names, disabled edge states and keyboard behavior; this change only isolates their state owner. Existing 16px geometry is unchanged. | None in this ownership PR.                                                      |
| `src/engines/Simulator/components/GridCell/IndependentGridCell.tsx:197` | Existing expand icon      | fix candidate    | Existing iconOnly Button passes its glyph as children, while default iconOnly rendering expects icon. This predates the ownership diff.                                     | Move the glyph to icon in a separate presentation fix with visual verification. |
| `src/engines/Simulator/components/SubagentPipCard.tsx:643`              | Embedded cell             | keep with reason | Supplies the authoritative session identity to the existing shared cell; no new action control or styling.                                                                  | None.                                                                           |
| `src/engines/Simulator/apps/backgroundTasks/BackgroundTasksApp.tsx:111` | Background-session cell   | keep with reason | Uses the same shared cell and session identity contract as the main grid.                                                                                                   | None.                                                                           |

Verdict totals: **1 fix candidate**, **4 keep with reason**, **0 abstract**.

Reviewed changed production JSX and surrounding controls for raw buttons, native element creation, substitute click targets and inputs. No new bypass is introduced. No styles or colors are added by this diff; no repeated visual scaffold is introduced. The existing expand-icon issue is recorded without mixing a separate presentation fix into replay ownership. Native visual screenshots were not captured.
