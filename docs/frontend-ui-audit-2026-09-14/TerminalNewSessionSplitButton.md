# TerminalNewSessionSplitButton UI audit

Scope: new-terminal action presentation in sidebar and header, and shared split menu hover behavior. Checked D1–D5 against shared Button and SplitButton props. Header tooltips use the shared shortcut-tooltip presentation with labels only; native header titles are suppressed while accessible labels remain. Sidebar titles remain unchanged.

| Line                                                                                                 | Element                           | Verdict          | Reason                                                                                                                                                                      | Suggested change |
| ---------------------------------------------------------------------------------------------------- | --------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/WorkStation/shared/TerminalNewSessionSplitButton.tsx:138`                               | Single terminal creation action   | keep with reason | Uses tertiary/soft-no-drop with existing sidebar or header hit area                                                                                                         | None             |
| `src/modules/WorkStation/shared/TerminalNewSessionSplitButton.tsx:164`                               | Terminal split creation action    | keep with reason | Both densities now use shared SplitButton soft-no-drop; retains sidebar width, main glyph stroke, click propagation guards and existing shell picker                        | None             |
| `src/components/SplitButton/index.tsx:139`                                                           | Split menu hover/focus appearance | keep with reason | Uses the stronger shared button-hover fill on the menu segment while group hover keeps the main segment on lighter button-hover-no-drop; covered for both rendered segments | None             |
| `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/TerminalMainContent/index.tsx:162` | Header action tooltips            | keep with reason | Shared ToolbarTooltip renders labels without shortcut props; native title suppressed on new/stop/close and accessible names retained; info keeps its process-details popup  | None             |

Verdict totals: **0 fix**, **4 keep with reason**, **0 abstract**.

No raw button or substitute clickable control introduced. Existing custom layout inside SplitButton is required to position the independent menu segment over caller-sized compound geometry; terminal shell menu rows preserve their existing shared dropdown surface. No polling, subscriptions, effects or retained state were modified. Desktop visual verification was not performed because computer control was not requested.

Open-state follow-up: shared SplitButton owns persistent highlighting for enabled neutral soft-no-drop controls while menuOpen is true. The main segment retains the lighter fill and primary-6 text, and the menu segment retains the heavier fill and primary-6 chevron. Shared main-segment active classes intentionally override resting/hover presentation so the icon stays primary during hover. The terminal caller no longer supplies an open-state class override. Tests cover enabled and disabled open states.
