# SendOnEnterPill UI audit

| Line                             | Element                                | Verdict          | Reason                                                                                                                                                                                                                     | Suggested change |
| -------------------------------- | -------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `SegmentedTextPill/index.tsx:74` | Segmented option button                | keep with reason | This design-system primitive uses the shared `Button` with custom layout and appearance because the compound control owns its direct children, centered content, pill geometry, and selected surface.                      | None.            |
| `SendOnEnterPill.tsx:34`         | Send-shortcut segmented control        | keep with reason | Reuses the shared `SegmentedTextPill` and its shadowless 32px, regular-weight size matching default settings inputs; the primitive provides the shared `Button` implementation, pressed state, and accessible group label. | None.            |
| `ChatPanelAppearanceTab.tsx:133` | Chat appearance send-shortcut control  | keep with reason | Reuses `SendOnEnterPill` so both settings surfaces share the same platform-aware options and boolean mapping.                                                                                                              | None.            |
| `GeneralSection.tsx:405`         | General settings send-shortcut control | keep with reason | Reuses `SendOnEnterPill` instead of duplicating a segmented-control implementation in the general settings page.                                                                                                           | None.            |
| `GeneralSection.tsx:479`         | Update-channel control                 | keep with reason | Reuses the same shared 32px `SegmentedTextPill` presentation as the send-shortcut choice while preserving the updater's resolved-channel and refresh behavior.                                                             | None.            |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.
