# Shortcut presentation consolidation UI audit

| Line                                                                                                                                                                                                                                                                                                                                                                                                                                           | Element                                     | Verdict          | Reason                                                                                                                                                                                    | Suggested change                                                                                                                          |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/KeyBadge/index.tsx` (removed); `src/modules/MainApp/Settings/sections/ShortcutsSection/ShortcutRecorder.tsx:103`; `src/modules/shared/DevTools/APICallPanel/components/PanelHeader.tsx:74`; `src/modules/shared/DevTools/ComponentIssueModal/index.tsx:283`; `src/scaffold/NavigationSidebar/components/NavigationMenu/NavigationMenu/renderRowParts.tsx:16`; `src/modules/WorkStation/shared/QuickActionsPanel/index.tsx:105` | Parallel and raw shortcut presentations     | fix              | `KeyBadge` duplicated parsing and icon rendering, the `workStation` variant duplicated the default style, and several surfaces contained raw shortcut presentations                       | Route every rendered keyboard chord through `KeyboardShortcut`, delete `KeyBadge` plus its bespoke SCSS, and remove the redundant variant |
| `src/components/KeyboardShortcut/index.tsx:343`                                                                                                                                                                                                                                                                                                                                                                                                | Shortcut size tokens                        | keep with reason | Glyph-slot widths continue to match each SVG size while shortcut text renders one pixel smaller at every presentation level; pill sizes use shared asymmetric padding for optical balance | None                                                                                                                                      |
| `src/modules/shared/DevTools/APICallPanel/components/PanelHeader.tsx:65`; `src/features/CodeMirror/SqlEditor/index.tsx:239`                                                                                                                                                                                                                                                                                                                    | Native title and translated tooltip strings | keep with reason | Browser `title` attributes and translated string interpolation cannot render React nodes; they are accessible text, not parallel visual shortcut components                               | None                                                                                                                                      |
| `src/scaffold/NavigationSidebar/components/NavigationMenu/config.ts:95`                                                                                                                                                                                                                                                                                                                                                                        | Sidebar trailing metadata                   | fix              | The old `shortcut` field also carried relative ages such as `21h`, obscuring which values should use the shortcut design system                                                           | Keep `shortcut` for keyboard chords and move non-keyboard metadata to `trailingLabel`                                                     |

Verdict totals: **2 fix**, **2 keep with reason**, **0 abstract**.

## Verification

- Repository sweep found no production `<kbd>` outside `src/components/KeyboardShortcut/index.tsx` and no remaining production `KeyBadge` definition or import.
- Targeted KeyboardShortcut, Settings shortcut recorder, NavigationMenu, and sidebar-builder tests: 51 passed across 7 files.
- ESLint passed for the changed TypeScript and TSX files.
- `pnpm typecheck:fast` passed in the isolated worktree.
- Desktop visual verification was not performed because Computer Use was not requested.
