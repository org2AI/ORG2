# Source control empty states UI audit

| Line                                                                                                               | Element                      | Verdict          | Reason                                                                                                                                                                                       | Suggested change |
| ------------------------------------------------------------------------------------------------------------------ | ---------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/components/Placeholder/index.tsx:168`                                                                         | Placeholder action           | keep with reason | Uses the shared `Button`; the placement-aware default maps sidebar actions to the existing 28px `small` size and preserves caller overrides.                                                 | None.            |
| `src/modules/WorkStation/shared/StatusBar/GitInitializationStatusMenu.tsx:115`                                     | Git initialization menu item | keep with reason | Uses the shared `Button` with `layout="custom"` and `appearance="custom"` because the reusable dropdown item requires direct icon-and-label children and the shared dropdown surface tokens. | None.            |
| `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/GitCommitDetailContent/index.tsx:309`            | No-changes detail-pane icon  | keep with reason | Reuses the shared `Placeholder`, `HugeiconsIcon`, and `PLACEHOLDER_TOKENS.detailIconSize`; no new visual primitive or arbitrary color was introduced.                                        | None.            |
| `src/modules/WorkStation/shared/DiffSectionList/index.tsx:365`                                                     | Diff list empty state        | keep with reason | The reusable list accepts an optional React node and forwards it to the shared detail-panel `Placeholder`, keeping empty-state layout ownership in one component.                            | None.            |
| `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/SourceControlMainContent/AllChangesView.tsx:204` | All Changes no-changes icon  | keep with reason | Uses the shared `Archive04Icon`, `HugeiconsIcon`, and `PLACEHOLDER_TOKENS.detailIconSize` so staged and unstaged empty states match the large single-file detail treatment.                  | None.            |
| `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/SourceControlSelectionPlaceholder.tsx:33`        | Focus-mode empty icon size   | keep with reason | Reuses the shared detail-icon size token while preserving the existing source-control glyph and opacity treatment.                                                                           | None.            |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**, **0 watch**.
