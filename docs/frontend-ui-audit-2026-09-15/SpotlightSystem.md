# Spotlight system UI audit

> Historical audit snapshot. Approved fixes and current verification are recorded in [SpotlightCleanup.md](SpotlightCleanup.md).
> Current working tree, 2026-09-15. Source/AST inspection; no desktop UI control or source fixes.

| Line                                                                                                                                          | Element                              | Verdict          | Reason                                                                                                                                                                 | Suggested change                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/scaffold/GlobalSpotlight/components/SpotlightItemRow.tsx:379`                                                                            | Shared actionable result row         | fix              | Clickable div bypasses the explicit shared Button convention; no row control semantics. Existing pin and checkbox controls prevent a naive enclosing-button conversion | Build a compound row with shared Button activation and sibling pin/checkbox controls; preserve keyboard ownership, hover, context menu and measured geometry |
| `src/scaffold/GlobalSpotlight/components/SpotlightItemRow.tsx:367`                                                                            | Enabled tag color                    | fix              | `text-slate-600` bypasses semantic theme colors, unlike neighboring disabled/tag text                                                                                  | Use the appropriate `text-text-*` token; verify contrast across themes                                                                                       |
| `src/scaffold/GlobalSpotlight/components/ManageAgentsFooterAction.tsx:21`; `ManageModelsFooterAction.tsx:31`; `ManageKeysFooterAction.tsx:30` | Three footer navigation presets      | abstract         | All repeat close, optional action dispatch and navigation fallback around the same SpotlightFooterAction                                                               | One typed footer-navigation adapter accepting label, action id and fallback destination; retain readable presets where useful                                |
| `src/scaffold/GlobalSpotlight/shell/SpotlightShellChrome.tsx:130,147,180`                                                                     | Shell click handlers                 | keep with reason | These route focus from blank panel/footer space and do not substitute for a visible action button                                                                      | Keep separate from actionable row conversion                                                                                                                 |
| `src/scaffold/GlobalSpotlight/components/SpotlightTabs.tsx:123`                                                                               | Shared Button with custom tab layout | keep with reason | Tabs are an explicitly allowed compound-control boundary; retains tab-specific state and keyboard behavior                                                             | Keep                                                                                                                                                         |
| `src/scaffold/GlobalSpotlight/components/SpotlightFooterAction.tsx:21`                                                                        | Shared footer pill                   | keep with reason | Reusable local shell intentionally owns a 36px pill and full-height inner hit area; shared Button still owns native control semantics                                  | Keep geometry while consolidating callers; no per-site styling rewrite                                                                                       |
| `src/scaffold/GlobalSpotlight/shared/SpotlightInput.tsx:91`; `components/SpotlightSearchBar.tsx:181`                                          | Search inputs                        | keep with reason | Both use shared Input with bare appearance and explicit callback adaptation; path-aware search bar and simple search input serve distinct chrome                       | Keep; remove unused loading prop separately                                                                                                                  |
| `src/scaffold/GlobalSpotlight/components/SpotlightItemRow.tsx:223`; `components/SpotlightDetailPane.tsx:110`                                  | Viewport-bounded width expressions   | keep with reason | Responsive max-width calculations preserve truncation/popover placement and are not interchangeable with a fixed spacing token                                         | Keep                                                                                                                                                         |

Verdict totals: **2 fix**, **5 keep with reason**, **1 abstract**.

The footer abstraction is one sweep candidate spanning three files. No site-by-site fixes made. Arbitrary pixel counts alone were not promoted to findings; runtime visuals and theme contrast were not measured. Production source/AST checks found no raw button/input/textarea/select JSX in Spotlight.
