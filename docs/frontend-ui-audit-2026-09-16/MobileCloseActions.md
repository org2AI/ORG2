# Mobile close actions UI audit

| Line                                                  | Element                                                              | Verdict          | Reason                                                                                                                                                            | Suggested change                                                                                                |
| ----------------------------------------------------- | -------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `MobileProfileSheet.tsx:56`, `SessionsScreen.tsx:535` | Close and search-clear button geometry                               | fix              | Default text-button inline sizing overrode mobile CSS dimensions and padding, compressing the icon; the user explicitly requested this iOS sweep                  | Applied iconOnly, circular shape, token-sized square hit area and zero padding                                  |
| `MobileHeaderIconButton.tsx:16`                       | Profile, tool detail, change review and image viewer header controls | abstract         | Four mobile sheets need the same close affordance; shared Button remains the owning primitive                                                                     | Extracted one control with a 20px non-shrinking glyph, stroke 2, 44px mobile touch token and theme-token colors |
| `MobileHeaderIconButton.tsx:24`                       | Button semantics and focus                                           | keep with reason | Uses standard shared Button props, accessible name, hidden decorative SVG and forwarded ref; existing Modal owns Escape/focus restoration                         | Preserve                                                                                                        |
| `mobileHeaderIconButton.scss:5`                       | Portal sizing variable                                               | keep with reason | Token is defined on the control itself because portals do not inherit the mobile shell                                                                            | Preserve                                                                                                        |
| `MobileComposerImagePreview.tsx:40`                   | Attachment removal badge                                             | keep with reason | Already uses iconOnly and circular geometry; small glyph sits within a larger touch target and fits the thumbnail overlay                                         | Preserve purposeful compact treatment                                                                           |
| `src/components/BottomSheet/index.tsx:134`            | Shared bottom-sheet close                                            | keep with reason | Custom-layout primitive explicitly owns equal width/height and zero padding, so it does not have the text-layout compression bug; shared with other product flows | Preserve existing shared primitive in this scoped bug fix                                                       |

Verdict totals: **1 fix**, **4 keep with reason**, **1 abstract**.

The authorized sweep covers MobileRemote close glyphs, plus shared BottomSheet and Modal close entry points. No new native buttons or substitute clickable elements were introduced. Desktop Modal presentation is unchanged. No async/state lifecycle was changed.

Verification: 53 existing tests across SettingsTab, SessionsScreen.search, MobileChangeReview.panel, MobileChangeReview and MobileMessageImages passed (the 21 tests in the two changed test files were rerun after updating the shared size-token expectation and adding image close/reopen coverage). TypeScript and scoped ESLint passed. The live mobile webpack build compiled successfully. Simulator app was relaunched to load the real implementation; automatic in-app navigation/visual inspection remains limited by simulator automation availability.
