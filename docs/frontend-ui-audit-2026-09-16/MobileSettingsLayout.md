# Mobile Settings layout UI audit

| Line                    | Element                              | Verdict          | Reason                                                                                                                                                | Suggested change                                                                                                                                                 |
| ----------------------- | ------------------------------------ | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SettingsTab.tsx:82`    | Settings heading and section spacing | fix              | Repeated title/card/title gaps made a four-row page unnecessarily scattered                                                                           | Compact the page heading; use one account card and one grouped preferences card with a raised theme surface, no visible outer border and subtle inset separators |
| `SettingsTab.tsx:115`   | Desktop name and presence            | fix              | A long hostname plus inline status broke into awkward fragments                                                                                       | Keep hostname and text-labelled presence in one flex row; ellipsize only the name and retain full hostname in DOM/title                                          |
| `SettingsTab.tsx:90`    | Card/row scaffolding                 | keep with reason | Reuses SectionContainer and SectionRow; child section boundaries retained for controls and test selectors while outer container owns the card surface | None                                                                                                                                                             |
| `SettingsTab.tsx:158`   | Theme control                        | keep with reason | Existing shared Select preserves its keyboard, popup, pending/error and persistence behavior                                                          | Keep bare appearance and a minimum 44px touch area                                                                                                               |
| `SettingsTab.tsx:95`    | Account entry                        | keep with reason | Existing MobileProfileEntry uses shared Button, Profile sheet and confirmation flow                                                                   | Use a single-line name and trailing Profile hint, with a 32px avatar; preserve the existing shared Button and accessible dialog label                            |
| `SettingsTab.tsx:267`   | Optional help/revoke actions         | keep with reason | Existing shared Buttons retain handlers and semantic danger styling, with 44px touch minimum                                                          | None                                                                                                                                                             |
| `SettingsTab.tsx:222`   | Row icon treatment                   | keep with reason | Shared icon component, 18px glyph and local 1.25rem alignment slot without a decorative well create a compact repeated label pattern                  | Reuse SettingsLabel for all three rows                                                                                                                           |
| `mobileSettings.scss:1` | Scoped theme/geometry styles         | keep with reason | Uses shared color, typography and mobile spacing tokens; Settings-only scope avoids changing other pages                                              | None                                                                                                                                                             |

Verdict totals: **2 fix**, **6 keep with reason**, **0 abstract**.

Validation: 8 existing Settings behavior tests pass (profile/help, focus restoration, signout confirmation, theme selection, optional revoke, permissions and endpoint redaction); TypeScript and scoped ESLint pass. Existing assertions were updated only for the deliberately split desktop status and removal of redundant Appearance heading. No new behavior tests added for a pure layout change. All production actions remain shared controls; no raw button/input or substitute clickable elements introduced.

All colors adapt through tokens; light/dark visual validation, narrow viewport and enlarged text remain unverified because simulator coordinate actions continue to fail with `noWindowsAvailable`. Development webpack compilation succeeds. This is a real MobileRemote app change, not a separate preview.

Follow-up: account and desktop rows are now single-line; long values ellipsize. Settings rows use a 52px minimum, account content fits in 56px, and the theme trigger keeps its 44px target. No colors are hardcoded; card surface uses `--color-bg-3`. Profile row is used only by Settings; avatar-style entries and the Profile sheet remain unchanged. Re-ran the same 8 Settings behavior tests, scoped ESLint and TypeScript successfully.
