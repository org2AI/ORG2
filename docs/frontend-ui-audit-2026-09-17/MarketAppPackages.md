# Market App Packages UI audit

| Line                        | Element                                  | Verdict          | Reason                                                                                                                                                                                                                   | Suggested change                                                      |
| --------------------------- | ---------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| `AppConnectionPage.tsx:72`  | Native Package/model eligibility         | fix              | Engine protocol alone did not establish support for the exact native App; selection now shares exact-client and availability validation with the configuration bridge, with authoritative Rust checks before writes/open | Implemented and regression tested                                     |
| `ConnectionCards.tsx:33`    | Package toggle cards                     | keep with reason | Shared Button uses primary/secondary outline treatments and aria-pressed; array selection preserves the existing single-selection caller contract                                                                        | None                                                                  |
| `ConnectionCards.tsx:46`    | Custom card layout                       | keep with reason | Compound title, active marker and optional description require direct children and content-driven height; standard fixed-height action geometry would clip multi-line cards                                              | None                                                                  |
| `ConnectionCards.tsx:57`    | Long and duplicate Package names         | keep with reason | Truncation has a full-title tooltip; duplicate titles have an existing localized ordinal; native catalogs add a stable distinguishing suffix                                                                             | None                                                                  |
| `AppConnectionPage.tsx:433` | Default Package/model select             | fix              | Shared Select requires `ariaLabel`; the old `aria-label` prop was ignored. Default choices also need the same duplicate-Package ordinal as their cards so overlapping models remain distinguishable                      | Use the shared prop and ordinal label; covered by rendered assertions |
| `AppConnectionPage.tsx:389` | Eight-Package limit and selection state  | keep with reason | Existing selections remain deselectable at the bound, new choices explain the limit, and configured Packages initialize editing                                                                                          | None                                                                  |
| `AppConnectionPage.tsx:258` | Apply/open/restore controls and failures | keep with reason | Shared Button retains busy/disabled/conflict handling; loading, empty and fetch failure states use existing localized copy                                                                                               | None                                                                  |

Verdict totals: **2 fix**, **5 keep with reason**, **0 abstract**.

Changed production controls were inspected as JSX, including the shared Button implementation and presentation contract. No new raw button/input, clickable div/span, arbitrary color, or new per-site control family was introduced. The existing responsive one/two-column card grid is retained. Relevant frontend suites passed (7 files / 34 tests). The final Instance 89 binary was launched through native UI: all three App tabs rendered, and Configure → Market showed the new eight-Package help and signed-out empty state. A dark-theme screenshot was captured in the task. Populated Package choices, light theme, request errors and actual native App interactions remain unverified; fixtures do not replace those checks.
