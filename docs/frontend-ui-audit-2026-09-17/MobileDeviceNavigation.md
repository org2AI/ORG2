# Mobile device navigation UI audit

| Line                                                                      | Element                      | Verdict          | Reason                                                                                                         | Suggested change                                                                                                             |
| ------------------------------------------------------------------------- | ---------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `src/modules/MobileRemote/components/MobileTabBar.tsx:21`                 | Top-level device destination | fix              | Device information duplicates the session switcher and settings; user explicitly requested consolidation       | Removed devices tab and type variant; kept Sessions and Settings                                                             |
| `src/modules/MobileRemote/screens/settings/SettingsTab.tsx:87`            | Connection row               | fix              | A passive desktop summary cannot reach device management                                                       | Entire row opens Connection & devices with an accessible name and chevron; current desktop remains single-line with ellipsis |
| `src/modules/MobileRemote/screens/settings/mobileSettings.scss:81`        | Custom Button layout         | keep with reason | Compound label/value/chevron row owns flexible columns and token-based 44px geometry; still uses shared Button | Retain custom layout and visible keyboard focus                                                                              |
| `src/modules/MobileRemote/screens/devices/ConnectionDevicesScreen.tsx:72` | Settings destination         | keep with reason | Reuses MobileTopBar and Section primitives; real inventory/current selection still comes from the provider     | Keep status and switch error/pending UI; add return and existing QR pairing entry                                            |
| `src/modules/MobileRemote/components/SessionDeviceTabs.tsx:40`            | Daily computer switcher      | keep with reason | Already uses shared TabPill and provider-owned selection; remains available from Sessions                      | No change                                                                                                                    |

Verdict totals: **2 fix**, **3 keep with reason**, **0 abstract**.

No new native React buttons, inputs or substitute clickable spans/divs. Added actions use shared Button. Settings descriptions have no sentence-ending punctuation. Theme surfaces remain token-based; both locale labels updated. Production screenshot confirms the real simulator app now has two tabs. Simulator coordinate interaction fails with `noWindowsAvailable`; subpage visual QA in both themes is not claimed.
