# Mobile Profile sheet UI audit

| Line                                                                     | Element                           | Verdict          | Reason                                                                                                                                                                                       | Suggested change                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------ | --------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/modules/MobileRemote/_mobileUiTokens.scss:67`                       | Sheet surface and geometry        | fix              | Profile inherited desktop backdrop/shadows and had independently hardcoded spacing/radius/height values                                                                                      | Added portal-scoped sheet aliases derived from shared theme colors, mobile spacing/touch scale and shared shadow; backdrop uses Tailwind bg-bg-overlay token via @reference + @apply because it is an inline theme value rather than a runtime CSS variable |
| `src/modules/MobileRemote/components/profile/mobileProfile.scss:130`     | Identity and action groups        | fix              | Large gaps and flat full-width separators made the modal tall and visually disconnected                                                                                                      | Compact identity, semantic group captions, rounded group surfaces, inset separators and 48px minimum action rows; hint outside destructive action card                                                                                                      |
| `src/modules/MobileRemote/components/profile/MobileProfileSheet.tsx:215` | Shared Button custom layout       | keep with reason | Compound icon/text/trailing-glyph menu row needs caller-owned columns, wrapping, rounded edge rows and CSS sizing; default text-button wrapper imposed inline desktop padding and truncation | Retain shared Button native disabled/click semantics, use custom layout with documented reason and explicit focus style                                                                                                                                     |
| `src/modules/MobileRemote/components/profile/mobileProfile.scss:64`      | Radius/header size overrides      | keep with reason | Modal radius and PanelHeader text size are inline desktop numbers; local scoped !important is needed without changing desktop consumers                                                      | Keep two scoped overrides mapped to semantic tokens                                                                                                                                                                                                         |
| `src/modules/MobileRemote/components/profile/MobileProfileSheet.tsx:89`  | Avatar/icon numeric sizes         | keep with reason | Shared PersonAvatar and Hugeicons APIs accept numeric pixel sizes; 48px avatar / 20px row glyph / 18px trailing glyph fit their fixed roles                                                  | Preserve numeric primitive inputs; surfaces, spacing and typography remain theme/token owned                                                                                                                                                                |
| `src/modules/MobileRemote/components/profile/mobileProfile.scss:255`     | Reduced motion and bounded scroll | keep with reason | Existing shared Modal owns focus, Escape, scroll locking and body overflow; animation is disabled for reduced motion and sheet respects safe-area/dynamic viewport                           | Preserve lifecycle and accessibility behavior                                                                                                                                                                                                               |

Verdict totals: **2 fix**, **4 keep with reason**, **0 abstract**.

Scope is the Profile modal and its Help page, not a global desktop Modal redesign. No persistent state, request, timer, subscription or account-action behavior changed. No native React action controls added; all actions use shared Button or existing wrappers. No hex/rgb palette was introduced into the sheet. Settings-entry/avatar-trigger presentation remains outside this modal-specific change.

Verification: `pnpm test src/modules/MobileRemote/components/profile/MobileProfileEntry.test.ts src/modules/MobileRemote/screens/settings/SettingsTab.test.ts` — 31 tests passed, including focus return, Help/back, Escape, signout confirmation/cleanup, external action failure/retry and account replacement. TypeScript/scoped ESLint and live webpack compilation checked. Simulator automated coordinates currently fail with noWindowsAvailable, so visual checks of the modified sheet in light/dark, long labels and reduced height are not claimed.

Final verification: `pnpm exec tsc --noEmit --pretty false` and scoped ESLint passed after correcting two HTMLButtonElement annotations in the previously edited device-screen tests. The Profile stylesheet compiles successfully with an explicit Tailwind reference. `git diff --check` passed. No account action was executed on the real device.
