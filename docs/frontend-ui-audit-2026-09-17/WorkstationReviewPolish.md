# Workstation review polish UI audit

| Line                                                                                                                      | Element                 | Verdict          | Reason                                                                                                                                                                             | Suggested change |
| ------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/components/VirtualList/index.tsx:258`                                                                                | Virtual row positioning | keep with reason | Row offsets are runtime geometry owned by TanStack Virtual; inline `top`/`transform` values cannot be represented by static design-system classes.                                 | None.            |
| `src/features/CodeMirror/Diff/index.tsx:130`                                                                              | Collapsed-row theme     | keep with reason | This is a CodeMirror bridge layer. The one-line and two-line heights follow editor typography via `lh`, while app color variables preserve theming.                                | None.            |
| `src/features/CodeMirror/Diff/collapsedGutter.ts:188`                                                                     | Native collapse buttons | keep with reason | `GutterMarker.toDOM` is a CodeMirror-owned non-React boundary, so the shared React `Button` cannot render here; the controls retain native button semantics and accessible labels. | None.            |
| `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/FilePreviewContent/ImagePreview/ImageBottomBar.tsx:163` | Image-preview actions   | keep with reason | The toolbar already uses the shared `Button`, semantic pressed/disabled states, and the shared `HEADER_ICON_SIZE` token.                                                           | None.            |

Verdict totals: **0 fix**, **4 keep with reason**, **0 abstract**.
