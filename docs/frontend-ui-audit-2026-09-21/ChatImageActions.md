# Chat image actions UI audit

| Line                                                                                                                                                   | Element                                | Verdict          | Reason                                                                                                                                                   | Suggested change                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/engines/ChatPanel/InputArea/components/ImageAttachmentPreview.tsx:64`; `src/engines/ChatPanel/InputArea/components/EditModeImageThumbnail.tsx:43` | Draft image preview targets            | fix              | Implemented: use shared Button for keyboard-accessible preview/menu actions; remove remains a sibling Button, avoiding nested buttons                    | Completed in this feature                                                                                                                                                        |
| `src/components/ImageActions/useImageActions.ts:78`                                                                                                    | Native image context menu              | keep with reason | Reuses popupNativeMenu, its single-popup gate and native resource cleanup; menu items are OS controls, not React button bypasses                         | None                                                                                                                                                                             |
| `src/components/MarkDown/MarkdownLocalImage.tsx:227`; `src/engines/ChatPanel/ChatImageThumbnail/index.tsx:143`                                         | Image buttons with custom layout       | keep with reason | Intrinsic markdown geometry and fixed thumbnail geometry must retain direct image children; shared default button layout wraps text and is inappropriate | None                                                                                                                                                                             |
| `src/scaffold/ImagePreviewOverlay/index.tsx:52`                                                                                                        | PreviewAction toolbar component        | keep with reason | Reuses Button and PANEL_HEADER_TOKENS; named component keeps callbacks out of render-time function evaluation                                            | None                                                                                                                                                                             |
| `src/components/MarkDown/MarkdownLocalImage.tsx:274`                                                                                                   | Loading placeholder click interception | keep with reason | Existing handler only prevents navigation through a wrapping markdown link; it is not a substitute action control                                        | None                                                                                                                                                                             |
| `src/engines/ChatPanel/InputArea/index.tsx:255`                                                                                                        | Hidden file input                      | keep with reason | Documented native file-picker boundary; no shared text/form field is bypassed                                                                            | None                                                                                                                                                                             |
| `src/features/SessionCreator/components/ImageThumbnailRow.tsx:24`; draft/edit thumbnail components above                                               | Three existing thumbnail shells        | abstract         | Existing shared dimensions/remove affordance recur in session creation and two composer views; a broader component sweep is outside this menu feature    | Follow-up candidate: reusable ImageAttachmentThumbnail with source, label and optional remove callback; leave the session-creation surface unchanged pending a separate decision |

Verdict totals: **1 fix**, **5 keep with reason**, **1 abstract**.

AST inspection of all changed production TSX files found no raw button JSX,
native button creation, or newly introduced substitute clickable elements.
The two remaining native/interception sites are explained above. No shared input
or design-token configuration changes are needed. All 15 locale catalogs include
the four new image-action strings.

Rendered component tests cover keyboard menu entry, menu capabilities, modal
closure before focus, original-image selection after gallery navigation, disabled
copy/add options and unchanged click-to-preview behavior. Native macOS menu
appearance, light/dark screenshots, and clipboard/Finder/save-dialog interaction
against a newly built app remain unverified: the running ORG2 window is a packaged
`tauri://localhost` application, not a dev server serving this working tree.
