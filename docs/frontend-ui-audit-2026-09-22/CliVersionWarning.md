# CLI version warning UI audit

| Line                                                                               | Element                    | Verdict          | Reason                                                                                                                                                      | Suggested change |
| ---------------------------------------------------------------------------------- | -------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/features/SessionCreator/variants/ChatPanel/ChatPanelCliVersionWarning.tsx:32` | Notice surface and copy    | keep with reason | Uses shared compact PageNotice, separates plain-language title from explicitly labelled current/latest versions, and disables unnecessary notice copying    | None             |
| `src/features/SessionCreator/variants/ChatPanel/CursorCliUpgradeButton.tsx:52`     | Upgrade action             | keep with reason | Shared secondary Button at small size; text label, native keyboard behavior and built-in loading/disabled state                                             | None             |
| `src/features/SessionCreator/variants/ChatPanel/ChatPanelCliVersionWarning.tsx:51` | Mute and recheck controls  | keep with reason | Existing shared Button and RefreshButton retain accessible labels, tooltips, disabling and click behavior; 14px glyphs match existing compact notice chrome | None             |
| `src/features/SessionCreator/variants/ChatPanel/ChatPanelCliVersionWarning.tsx:29` | Width and version wrapping | keep with reason | Existing content-width token and wrapping subtitle; no new arbitrary dimensions or colors                                                                   | None             |
| `src/components/PageNotice/index.tsx:133`                                          | Optional copy control      | keep with reason | Defaults to enabled for existing callers; this notice opts out through a shared prop instead of hiding internal DOM                                         | None             |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.

Production JSX inspected in all changed TSX files: no raw buttons, native button creation, substitute clickable elements, or form inputs added. The new action is deliberately Cursor CLI only; it is selected by canonical `cursor_cli`, not a translated display name. All 15 session locales carry the same new keys.

Rendered DOM tests cover the actual notice, shared buttons, launch loading, duplicate notices, remount, error/retry, closed-terminal recovery, CLI switch, and preserved recheck. Full Tauri visual verification, light/dark screenshots, and constrained-width screenshots were not run for this PR. Desktop dev startup was smoke-tested in the original working checkout, which also contained unrelated changes; it is not isolated PR validation. Shared tokens reduce styling risk but do not substitute for screenshot evidence.

Architecture review: layers 1–7 covered by successful typecheck, the production call chain, explicit idle/opening/opened states, canonical agent identity, and reuse of TerminalService. Layers 8–10 assessed: no wire/schema changes; existing dedicated-terminal initialization is reused; no multi-field resolver changes. Version observations remain owned by the existing scan/cache path; upgrade launch state is transient per-store state, not an installed-version assertion.
