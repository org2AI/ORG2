# Cloud session takeover placement UI audit

| Line                                                                                                                                                     | Element                      | Verdict          | Reason                                                                                                                                   | Suggested change                                                                                                                                          |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/scaffold/NavigationSidebar/connectors/WorkstationSidebarConnector/cloudSessionsSection.rowItemBuilder.tsx:276`; `cloudSessionNativeMenuItems.ts:72` | Separate takeover row action | fix              | User requested 接管 in the dropdown list instead of a dedicated row button.                                                              | Removed the row action and placed the same localized action with Login02Icon (enter/take over) in the canonical menu, shared by ellipsis and right-click. |
| `src/scaffold/NavigationSidebar/connectors/WorkstationSidebarConnector/cloudSessionsSection.remoteMenuItems.ts:99`                                       | Takeover handler             | keep with reason | Delegates to the existing runFork(row) path, preserving download gates, errors, and busy handling; pin and overflow remain row controls. | Keep existing behavior and shared dropdown rendering/tokens.                                                                                              |

Verdict totals: **1 fix**, **1 keep with reason**, **0 abstract**.

Verification: `pnpm exec vitest run --config config/vitest.config.ts src/scaffold/NavigationSidebar/connectors/WorkstationSidebarConnector/{cloudSessionNativeMenuItems,cloudSessionsSection.rowItemBuilder}.test.ts` passed 3 tests. They verify takeover is in the canonical menu and invokes its existing callback, while row actions contain only pin and overflow. `pnpm typecheck:fast`, targeted ESLint with `--max-warnings 0`, and `git diff --check` passed. No new raw controls or background resources; underlying takeover/sync implementation is unchanged. Desktop visual verification was not run under the user's computer-control preference.
