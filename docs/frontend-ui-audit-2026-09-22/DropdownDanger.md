# Dropdown danger prop UI audit

| Line                                                                                                                                                                                    | Element                      | Verdict          | Reason                                                                                                                                                                | Suggested change                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/Dropdown/DropdownActionItem.tsx:16`; `src/components/Dropdown/DropdownItem.tsx:92`; `src/scaffold/NavigationSidebar/connectors/useWorkstationSidebarContextMenu.ts:187` | Destructive dropdown actions | fix              | User requested a reusable danger prop and its use for Delete.                                                                                                         | Added optional danger prop to both shared row APIs, forwarded it through sidebar options, and enabled it for session Delete and draft removal. |
| `src/components/Dropdown/tokens.ts:308`                                                                                                                                                 | Destructive presentation     | keep with reason | Uses existing danger color tokens for label/icon and pointer/keyboard backgrounds; no raw color values or caller-specific overrides. Default rows are unchanged.      | Keep centralized danger tokens.                                                                                                                |
| `src/components/Dropdown/DropdownItem.tsx:225`; `src/components/Dropdown/DropdownActionItem.tsx:71`                                                                                     | Disabled/highlight semantics | keep with reason | Disabled rows omit danger styling and cannot activate; hoverable=false suppresses hover styling; active/highlighted danger rows use the matching semantic background. | Keep shared behavior and preserve native disabled handling.                                                                                    |

Verdict totals: **1 fix**, **2 keep with reason**, **0 abstract**.

Verification: `pnpm exec vitest run --config config/vitest.config.ts src/components/Dropdown/{DropdownActionItem.danger,tokens,ActionMenuSurface}.test.ts src/scaffold/NavigationSidebar/menus/SidebarMenu.test.ts src/scaffold/NavigationSidebar/connectors/__tests__/useWorkstationSidebarContextMenu.test.ts` passed 29 tests. Tests cover label/icon tokens on both row APIs, enabled versus disabled activation, default styling, DOM prop consumption, hoverable=false, and Delete option wiring. After a test-only lint repair, the three new danger tests were rerun successfully. `pnpm typecheck:fast`, targeted ESLint with `--max-warnings 0`, and `git diff --check` passed.

No new action controls or bypasses: DropdownActionItem continues to use Button; DropdownItem retains its existing primitive implementation. No lifecycle resources, persistence, or action behavior changes. Desktop visual verification was not run under the user's computer-control preference.
