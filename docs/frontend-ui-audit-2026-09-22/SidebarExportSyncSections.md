# Sidebar Export & sync sections UI audit

| Line                                                                                | Element                          | Verdict          | Reason                                                                                                                                                                                                 | Suggested change                                                                                                                           |
| ----------------------------------------------------------------------------------- | -------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/scaffold/NavigationSidebar/connectors/useWorkstationSidebarContextMenu.ts:253` | Separate export and sync flyouts | fix              | User requested one consolidated second level with two sections.                                                                                                                                        | Implemented one Export & sync submenu containing nonempty Export and Sync groups, separated by the shared divider. No third-level flyouts. |
| `src/scaffold/NavigationSidebar/menus/SidebarMenu.tsx:85`                           | Inline section rendering         | keep with reason | Uses the shared section-label and separator tokens and existing action rows; labelled role=group wrappers preserve menu keyboard traversal. No new controls, arbitrary sizing, or lifecycle resources. | Keep shared tokens, icons, eligibility, and callbacks.                                                                                     |

Verdict totals: **1 fix**, **1 keep with reason**, **0 abstract**.

`pnpm exec vitest run --config config/vitest.config.ts src/scaffold/NavigationSidebar/menus/SidebarMenu.test.ts src/scaffold/NavigationSidebar/connectors/__tests__/useWorkstationSidebarContextMenu.test.ts` passed 15 tests. Regression checks one flyout, two labelled sections, separator, keyboard traversal across sections, action execution/dismissal, and eligibility preservation. `pnpm typecheck:fast`, targeted ESLint with `--max-warnings 0`, and `git diff --check` passed. New parent label is localized in all 15 locales. No added runtime resources; visual desktop verification remains unrun under the user's computer-control preference. This supersedes the earlier two-submenu export/sync layout.
