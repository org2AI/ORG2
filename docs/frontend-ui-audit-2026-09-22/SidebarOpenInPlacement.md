# Sidebar Open in placement UI audit

| Line                                                                                                                                            | Element                          | Verdict          | Reason                                                                                                                                                                                                                   | Suggested change                                                                                                                                                               |
| ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/scaffold/NavigationSidebar/menus/SidebarMenu.tsx:105`; `src/scaffold/NavigationSidebar/connectors/useWorkstationSidebarContextMenu.ts:158` | Native-app destination placement | fix              | User requested Codex/Claude under Open in rather than at the menu root.                                                                                                                                                  | Move native-app ownership onto the Open in submenu option. Render the reused provider action only inside that submenu; remove the root action and request-level session field. |
| `src/components/Dropdown/ActionMenuSurface.tsx:178`                                                                                             | Asynchronous flyout fitting      | keep with reason | Provider availability can add a destination after the flyout opens. Opt-in size observation reuses existing token-based positioning and disconnects when closed; other submenu consumers retain their previous behavior. | Keep scoped resize observation, shared controls, icons, and spacing tokens.                                                                                                    |

Verdict totals: **1 fix**, **1 keep with reason**, **0 abstract**.

Verification: `pnpm exec vitest run --config config/vitest.config.ts src/scaffold/NavigationSidebar/menus src/scaffold/NavigationSidebar/connectors/__tests__/useWorkstationSidebarContextMenu.test.ts src/components/Dropdown/ActionMenuSurface.test.ts src/engines/ChatPanel/components/SessionHeaderActionsMenu.test.ts` passed 67 tests across 5 files. Regression verifies no native action at the root, lazy mounting under the Open in flyout, flyout resize observation, full-menu dismissal on selection, and observer cleanup. `pnpm typecheck:fast`, targeted ESLint with `--max-warnings 0`, and `git diff --check` passed.

No new native buttons or clickable substitutes: production action controls remain shared dropdown components. Performance lifecycle checks pass at the automated boundary; real-runtime measurement remains unrun under the user's no-computer-control preference. No polling was added. Desktop visual verification was not run. This placement supersedes the root-level placement described in SidebarSessionAppActions.md.
