# Agent Org finalization recovery UI audit

| Line                                   | Element                              | Verdict          | Reason                                                                                                                                                               | Suggested change |
| -------------------------------------- | ------------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `ChatStatusBanners.tsx:212`            | Finalizing status banner             | keep with reason | Reuses the shared segmented status bar and two-line content, has a polite live region, and adds no parallel banner styling.                                          | None.            |
| `ChatView.tsx:471`                     | Finalizing submit gate               | keep with reason | One run-phase fact covers group chat, coordinator, and member views while the existing Stop control remains available.                                               | None.            |
| `InputActions.tsx:94`                  | Stop while a finalizing draft exists | keep with reason | The shared action button keeps Stop available while the execution is active; the editable draft remains in place and cannot replace Stop with a blocked Send action. | None.            |
| `RejectedRequestRecoveryAction.tsx:85` | Restore action                       | keep with reason | Uses the shared `Button` with the surrounding compact action size and exposes a stable accessible text label.                                                        | None.            |
| `RejectedRequestRecoveryAction.tsx:98` | Draft collision preview              | keep with reason | Reuses the shared `Modal` and `PanelFooter`; explicit Append and Cancel prevent silent draft replacement.                                                            | None.            |
| `RejectedRequestRecoveryAction.tsx:23` | Preview text surface                 | keep with reason | Uses existing spacing, background, text, and radius utilities; the fixed 28-unit maximum height bounds long recovered text inside the modal.                         | None.            |
| `useInputAreaEffects.ts:102`           | Session-scoped restoration           | keep with reason | Reuses the existing composer mutation path and session guard; recovered images append through the shared attachment store.                                           | None.            |
| `OrgTaskAdapter.tsx:246`               | Recovery placement                   | keep with reason | The action appears only on the existing Task result card when durable provenance is present, avoiding a second notification surface.                                 | None.            |

Verdict totals: **0 fix**, **8 keep with reason**, **0 abstract**.
