# Workstation floating controls UI audit

| Line                                                              | Element                               | Verdict          | Reason                                                                                                                                                                               | Suggested change |
| ----------------------------------------------------------------- | ------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| `src/scaffold/WorkbenchChrome/WorkstationFloatingControls.tsx:30` | Float, dock and collapse actions      | keep with reason | Reuses `TabBarTrailingIconButton`, which delegates to shared `Button` with tertiary, small, iconOnly presentation. Localized titles provide accessible labels and standard tooltips. | None.            |
| `src/scaffold/WorkbenchChrome/WorkstationFloatingControls.tsx:83` | Collapsed launcher                    | keep with reason | Shared secondary Button with visible translated text and collapsed disclosure semantics. The shell owns positioning rather than duplicating floating geometry here.                  | None.            |
| `src/scaffold/WorkbenchChrome/WorkstationFloatingControls.tsx:50` | Control group                         | keep with reason | Shared NoDragRegion prevents OS title-bar dragging over actions. Spacing and glyph sizes reuse utilities and HEADER_ICON_SIZE tokens.                                                | None.            |
| `src/scaffold/WorkbenchChrome/StationHeaderControls.tsx:57`       | Shared entry policy                   | keep with reason | Both station modes use one entry. Detached windows omit presentation actions; floating presentation removes contradictory docked pane actions. No repeated per-mode control styling. | None.            |
| `src/scaffold/WorkbenchChrome/AgentStationTopHeader.tsx:116`      | Agent Station drag and chrome spacing | keep with reason | Floating presentation disables native OS dragging and global window-chrome reservations. Existing docked dimensions remain shared with the surrounding toolbar.                      | None.            |
| `src/modules/WorkStation/shared/TabBar/index.tsx:361`             | My Station drag and chrome spacing    | keep with reason | Optional presentation prop disables all three native drag regions while preserving one tab strip and drag-to-reorder implementation. Docked consumers retain the current default.    | None.            |
| `src/scaffold/WorkbenchChrome/WorkstationTabHeader.tsx:70`        | Tool header drag region               | keep with reason | The header remains the same element and disables only OS dragging outside docked presentation. Existing native platform distinction is preserved.                                    | None.            |

Verdict totals: **0 fix**, **7 keep with reason**, **0 abstract**.

Scope: controls, station headers and shared TabBar changes. Floating shell geometry and overlap behavior are verified by the shell implementation separately. Reviewed all five audit dimensions; no new raw inputs, hardcoded colors, or new reusable visual-pattern duplication. TypeScript AST inspection of the six changed production files found no raw button JSX, native button creation, or substitute clickable div/span/input controls. Control interaction and header regression tests pass (4 files, 20 tests); native Tauri dragging and visual validation require the full shell runtime.
