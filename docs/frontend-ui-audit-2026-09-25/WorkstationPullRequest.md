# Workstation Pull Request UI audit

| Line                                                                                  | Element             | Verdict          | Reason                                                                                                                                                                          | Suggested change |
| ------------------------------------------------------------------------------------- | ------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/scaffold/AppLayout/FocusedChatWorkstationRail/useWorkstationRailSections.ts:99`  | Pull request group  | keep with reason | Uses the existing section model, shared disclosure and rail rows in both responsive presentations; no parallel card styling                                                     | None             |
| `src/scaffold/AppLayout/FocusedChatWorkstationRail/useWorkstationRailSections.ts:145` | Hoisted panel title | keep with reason | Only the displayed header loses its inline label; repository and session headings remain visible when PR becomes the first group                                                | None             |
| `src/scaffold/AppLayout/FocusedChatWorkstationRail/WorkstationItemRow.tsx:51`         | PR title action     | keep with reason | Existing shared Button and WORKSTATION_TRAIL_ROW tokens own geometry, ellipsis, color and hit target; full title, number and separate CI status are supplied as accessible name | None             |
| `src/scaffold/AppLayout/FocusedChatWorkstationRail/WorkstationItemRow.tsx:132`        | Full-title tooltip  | keep with reason | Extends the existing Tooltip seam for resource titles; keyboard shortcut tooltips retain their original behavior; no native title duplication                                   | None             |
| `src/scaffold/AppLayout/FocusedChatWorkstationRail/index.tsx:210`                     | Collapse header     | keep with reason | Reuses WorkstationTrailHeader disclosure and terminal/rail controls; new section starts expanded using existing group state                                                     | None             |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.

All five dimensions reviewed. Changed production UI introduces no raw buttons, native button creation, substitute clickable elements, input controls, hardcoded color/geometry or additional reusable visual pattern. Existing `WorkstationSections`, `WorkstationItemRow`, shared `Button`, `Tooltip` and rail theme tokens provide the presentation. The existing `common:labels.pullRequest` translation is reused across locales.
