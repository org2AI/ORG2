# Shared selector continuity UI audit

| Line                                                                       | Element                                      | Verdict          | Reason                                                                                                                                                                                                           | Suggested change                                                                         |
| -------------------------------------------------------------------------- | -------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `src/components/SelectorPill/index.tsx:399`                                | Shared trigger and tooltip owner             | keep with reason | Always retaining Tooltip preserves the existing Button DOM when tooltip availability changes. A disabled Tooltip clones its child without adding a layout wrapper.                                               | Implemented stable owner; keep shared pill tokens.                                       |
| `src/components/SelectorPill/index.tsx:415`                                | Disabled explanation host                    | keep with reason | Native disabled buttons cannot receive focus. The optional span only hosts focus/hover explanation, never an action. Configure disabledTooltip in both states (empty string when no reason) to retain its shape. | Keep shared focus host and forward through PillGroup.                                    |
| `src/components/ModelSelectorPill/ModelSelectorPillView.tsx:392`           | Empty, combined and separate-effort triggers | keep with reason | All reuse SelectorPill or PillGroup backed by shared Button. Disabled state retains model and effort controls instead of changing component topology.                                                            | Keep caller-owned model/effort values; close menus when disabled.                        |
| `src/modules/MobileRemote/components/composer/MobileModelPicker.tsx:206`   | Mobile loading interaction                   | fix              | CSS pointer-events previously blocked pointer input without providing native disabled semantics, and transient disabled changed the editable component tree.                                                     | Fixed using shared disabled and disabledTooltip props; loading retains the action label. |
| `src/features/SessionCreator/variants/ChatPanel/chatPanelViewProps.ts:268` | Known branch during refresh                  | keep with reason | Upstream already gates branchLoading with !effectiveBranchName. Its meaningful initial loading state must remain.                                                                                                | No branch change.                                                                        |
| `src/components/ModelSelectorPill/ModelSettingsMenu.tsx:243`               | Menu dialog container                        | keep with reason | Existing div click handler only stops propagation inside a dialog; it is not a substitute action. Menu actions continue to use shared Button.                                                                    | No new raw controls.                                                                     |

Verdict totals: **1 fix**, **5 keep with reason**, **0 abstract**.

Scope: shared selector ownership, model/effort controls, mobile model picker and workspace/branch callers. Mode/permission/capability-driven visibility and genuine first-load lists are not rewritten. This is not a whole-app audit. The repeated tooltip concern is handled once at the existing SelectorPill seam, with no new control family or arbitrary visual tokens.

Production action inspection found no new raw button, native button creation, or clickable substitute. Tests use native button fixtures where needed. No inputs were added. Existing unrelated working-tree styling is excluded from this turn's audit.

Verification: 53 tests passed and 2 existing tests skipped across 10 related files; changed-file ESLint and fast typecheck passed. Native screenshot verification remains unavailable; no claim of measured native rendering improvement.
