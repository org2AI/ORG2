# Repeated Polling and Resource-Intensive Process Audit

- **Audit date:** 2026-07-23
- **Scope:** Production code under `src/` and `src-tauri/`
- **Method:** Static lifecycle and resource-retention audit using the ORG2 performance guard
- **Source changes:** None in the original audit commit; follow-up remediation is tracked below

## Executive summary

The audit identified several high-confidence performance failures alongside a larger set of bounded or feature-scoped recurring processes.

The highest-priority issues at the time of the original audit were:

1. Streaming sessions can rebuild and allocate a near-complete EventStore snapshot every 33 ms.
2. The work-item scheduler scans all projects and all work items every 30 seconds, with synchronous database work in an async task.
3. Work-item views poll complete message histories every 3 seconds and repeat Git/file calculations every 5–10 seconds.
4. Hosted-key activity, CSV drafts, approval details, navigation history, and several metadata maps have unbounded retention paths.
5. Every open PTY owns recurring reader and child-status polling, while frontend terminal caches are bounded mainly by entry count rather than total bytes.
6. Automation file watchers combine 200 ms polling with an unbounded event channel.
7. Frontend and backend diagnostics independently own recurring collection/flush schedulers.
8. Several feature pollers continue to wake while the document is hidden or have no maximum retry lifetime.

The most important architectural theme is that expensive work is often driven by elapsed time instead of authoritative invalidation. Several paths also use entry-count caps where byte-aware bounds are required.

## Post-audit remediation status

This section reconciles the original static findings with the commits that landed after `892bf176e515db31f4eefccb33263bd92badc988` and the current follow-up patch. Rows not listed here retain their original verdict.

| Area                                           | Verdict                | Evidence                                                                                                                                                                                                                                                                                                                 | Change or reason kept                                                                                                                                                                                                        | Verification                                                                                                                                                                                                                                                                             |
| ---------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Streaming EventStore snapshots                 | **fixed**              | `760af3df7` replaced repeated full-history streaming snapshots with versioned deltas containing changed/removed IDs and incremental order membership; a full snapshot remains only as the initialization/recovery baseline.                                                                                              | Keep the 33 ms coalescing boundary, but its work is now proportional to changed events rather than complete history.                                                                                                         | Rust delta tests cover changed-event compaction, membership positions, no-op deltas, and full-snapshot recovery; frontend coalescing tests cover materialization.                                                                                                                        |
| Diagnostics scheduler ownership                | **fixed**              | `1f9b47a01` removed the independent backend schedule and made the frontend visibility-aware scheduler the sole recurring owner. `a2f9ee84e` virtualized API/browser diagnostic lists and stops their pollers while closed/hidden.                                                                                        | One owner, non-overlapping passes, no hidden timer, and demand-driven rendered history.                                                                                                                                      | Diagnostics unit tests cover generation replacement and visibility lifecycle; API-call and browser lifecycle suites cover open/close behavior.                                                                                                                                           |
| External-history active-session refresh        | **fixed in follow-up** | The prior implementation throttled requests while unfocused but left the configured 3–60 second interval waking. The follow-up uses one recursive timer: configured cadence while focused, a real 60-second timer while unfocused, zero hidden timers, abort on hidden/unmount, and immediate focus/visibility catch-up. | Keep a cheap `(mtime, size)` signature probe for the currently open imported transcript; it avoids full parse/merge when unchanged.                                                                                          | Scheduler tests cover the 60-second background cadence, hidden suspension, visibility/focus catch-up, single-flight behavior, and disposal.                                                                                                                                              |
| External-history signature reuse               | **fixed in follow-up** | The auto-refresh hook now passes its already-observed transcript signature into the external-history adapter. A changed transcript performs the outer probe plus one post-load consistency stat instead of repeating the pre-load probe in each layer; a stable empty transcript is also remembered.                     | Preserve the post-load stat because it detects a writer racing the parse. Remove only redundant pre-load probes, keep loads single-flight, and leave a raced signature eligible for the next refresh.                        | Five adapter tests and nine auto-refresh tests pass. In a cleared real CTRL+5 window, normal probes stayed at the configured five-second cadence; one live transcript change caused one `es_set` and two metadata-index calls rather than the former four-stat refresh chain.            |
| Empty external-session round selector          | **fixed in follow-up** | A loaded external session with zero rounds kept a `Loader2.animate-spin` mounted indefinitely. WebView Inspector found exactly one active spinner on the stable empty view. The selector now animates only when an existing round is hydrating; the empty state uses the static chevron.                                 | ChatHistory remains the owner of initial loading feedback, so the round selector does not need an animation when `pageCount === 0`.                                                                                          | Before the fix, the empty session consumed 30.2% single-core CPU over 15 seconds; removing the class live dropped it to 7.08%. The final implementation measured 1.89% over 20 seconds at 661.1 MiB working set/368.5 MiB private, and three helper tests cover the state matrix.        |
| Large Codex external-history loading           | **fixed/bounded**      | `fb63a1177` loads a bounded initial turn window and fetches older turn bodies on demand. The Rust projection/cache path is bounded by session count, offsets, chunk count, and payload size; the frontend retains at most eight recent loaded historical turn bodies.                                                    | Demand-driven loading is required so multi-hundred-MiB rollout files do not block initial open or remain fully materialized.                                                                                                 | Real 960.9 MiB Codex rollout: warmed initial-window RPC 69.6 ms (106 chunks, 1,159,408 bytes); warm UI reopen 120–293 ms; older-turn page 450 ms; a second real external session opened in 337 ms. A 10-second post-open sample was stable (`working set -13.6 MiB`, `private -14 MiB`). |
| Loaded historical-turn lifecycle               | **fixed in follow-up** | `clearSessionAtom` already cleared the registry, but direct `loadSessionAtom` A→B switching did not. It now clears the departing session, and every async load captures a registry generation so a late completion cannot resurrect cleared metadata.                                                                    | Keep the eight-body per-session window and payload limits; add lifecycle eviction and stale-result rejection instead of a second global cache.                                                                               | Unit tests cover direct A→B eviction, explicit clear, and rejection of a stale completion after clear.                                                                                                                                                                                   |
| External-history reconciliation                | **fixed/bounded**      | Source reconciliation now uses exact next-due deadlines, signature probes, bounded in-flight work, and explicit refresh/focus invalidation rather than a 30-second due-check loop.                                                                                                                                       | Full rescans remain only for a source that is actually due or explicitly refreshed.                                                                                                                                          | Real CTRL+5 navigation produced one due-scan triplet at its deadline and no repeated 30-second traffic; large-history timing is recorded above.                                                                                                                                          |
| Data-source automatic scanning                 | **fixed**              | `useDataSourceAutoScan` arms the earliest source/probe deadline and re-evaluates only on focus/config invalidation. Hidden documents own no scan timer.                                                                                                                                                                  | Keep source-specific due intervals and one immediate catch-up on activation.                                                                                                                                                 | Scheduler tests cover deadline selection, invalidation, single-flight behavior, and hidden teardown.                                                                                                                                                                                     |
| Native event-cache synchronization             | **fixed/bounded**      | Persistence is event-driven with a 2-second quiet debounce and a 30-second maximum dirty-age deadline; the former fixed 30-second idle timer is gone.                                                                                                                                                                    | The maximum deadline preserves crash durability during continuous streaming without idle wakeups.                                                                                                                            | Session persistence tests and real visible/hidden idle profiling show no recurring cache-sync work.                                                                                                                                                                                      |
| Work-item scheduler/messages/files/diffs       | **fixed**              | Backend scheduling queries only due candidates, blocks database work off the async executor, and wakes from mutations/exact deadlines with a 30-minute safety rescan. Frontend messages/files/diffs share session event invalidation and suspend hidden work.                                                            | Keep one bounded safety rescan for durable rows that may be changed outside the normal mutation path.                                                                                                                        | Focused Work-item suites pass. Real CTRL+5 stayed at 32 cardinality-matched initial calls for 30 seconds with `0 likely polling`; 12 outbox and four project calls matched the 12 collaboration orgs and four projects in the local database.                                            |
| Provenance hook inbox                          | **fixed/bounded**      | The spool has 10,000-record/64 MiB quotas, bounded 1,000-entry enumeration, an overflow marker, and a loopback producer wake endpoint; the 15-second idle scan was removed.                                                                                                                                              | Overflow is explicit and bounded; event-driven wakes keep normal ingestion prompt.                                                                                                                                           | Rust tests cover quota and wake behavior; app idle profiling shows no provenance polling.                                                                                                                                                                                                |
| Remaining unbounded/byte-unbounded inventories | **fixed or bounded**   | Hosted-key activity, CSV/XLSX drafts, approval details, navigation, syntax, local-storage mirrors, terminal output, gateway/Feishu buffers, repo indexes, and live stream accumulators now have count and byte limits. Git/window/browser/config/LSP/memory pollers are non-overlapping and visibility-aware.            | Backend pinned EventStores and frontend snapshots retain count/event limits; byte-aware dematerialization remains a load-scale monitor because active data cannot be discarded without a persistence-backed paging contract. | Cache and lifecycle suites pass; targeted Rust suites pass. Real 20-second visible idle (without Computer Use capture) was 0.00% CPU at 685.3 MiB and stable; minimized hidden idle was also 0.00% CPU at 680.7 MiB.                                                                     |

## Scope and exclusions

Included:

- `setInterval`, recursive `setTimeout`, async loops, retry loops, heartbeats, long-polls, and watcher threads.
- Filesystem, database, Git, process, network, and model work initiated by recurring processes.
- App-lifetime and feature-lifetime caches, buffers, queues, maps, and snapshot stores.
- Large on-demand operations that can create significant CPU or RAM bursts.

Excluded:

- Unit, integration, E2E, and benchmark implementation timers unless they represented production behavior.
- One-shot UI debounces and bounded reveal/retry sequences.
- Development simulations that cannot run in production.
- Runtime CPU/RSS measurements in the original static pass; the post-audit section separately records the follow-up measurements that were subsequently collected.

## Critical and high-priority findings

| Area                                | Verdict                                    | Evidence                                                                                                                                                                                                                                                                                 | Change or reason kept                                                                                                                                  | Verification                                                                                                                                   |
| ----------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Streaming EventStore snapshots      | **fix — critical CPU/allocation**          | `src-tauri/src/agent_sessions/event_pipeline/commands/mod.rs:175-242` schedules streaming notifications every 33 ms. Snapshot emission traverses stored events, compacts data, builds vectors and lookup maps, sorts output, and creates simulator previews.                             | Make the streaming path incremental. Do not reconstruct or index complete session history for each batch. Minimize time holding the shared store lock. | Static call-path review shows up to approximately 30 snapshot passes per second per streaming session, with cost growing with session history. |
| Work-item scheduler                 | **fix — CPU/I/O and async blocking**       | `src-tauri/crates/agent-core/src/core/coordination/work_item_scheduler.rs:21-43` wakes every 30 seconds and reads all projects followed by all work items. Related labels/extras add per-item database work.                                                                             | Query only due/actionable work items. Move synchronous database/filesystem work behind `spawn_blocking`.                                               | Ambient work scales with total stored projects and work items rather than active items.                                                        |
| Work-item session messages          | **fix — CPU/RAM/IPC**                      | `src/modules/ProjectManager/WorkItems/components/AgentWorkflow/hooks/useSessionMessages.ts` polls every 3 seconds until a terminal state. `src-tauri/crates/agent-core/src/state/commands/session/persistence.rs:20` loads complete persisted messages, including content and tool data. | Replace with push/delta delivery or a cursor-based query. Bound frontend retention of text messages.                                                   | Complete message histories can be repeatedly queried, deserialized, transferred, filtered, and retained without a visibility pause.            |
| Work-item files and diff statistics | **fix — Git/process load**                 | `useSessionFiles.ts` polls every 5 seconds and recomputes session file state. `useLiveDiffStats.ts` polls every 10 seconds and invokes cumulative Git diff calculations.                                                                                                                 | Invalidate from session/Git events and pause while hidden. Share equivalent requests between consumers.                                                | Git and snapshot work repeats for every mounted active workflow.                                                                               |
| Hosted-key activity buffer          | **fix — unbounded RAM**                    | `src/engines/SessionCore/hooks/hostedKey/useHostedKeyActivitySync.ts:44-193` flushes at 1.5-second cadence in batches of 500. Failed batches are inserted back at the front while new events continue to arrive.                                                                         | Add event and byte caps, backpressure, durable spooling, and an explicit overflow policy.                                                              | A prolonged storage failure or sustained producer rate above the drain rate can grow the buffer without limit.                                 |
| CSV draft cache                     | **fix — unbounded RAM**                    | `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/FilePreviewContent/CsvTableView/index.tsx:37` retains module-level drafts with both edited and original rows. There is no entry or byte cap.                                                                           | Use byte-aware LRU eviction and per-file size limits. Clear drafts on file/session disposal where safe.                                                | Retention grows with every unsaved CSV and its row count.                                                                                      |
| XLSX draft cache                    | **fix — byte-unbounded RAM**               | `src/modules/WorkStation/CodeEditor/Panels/EditorMainPane/content/FilePreviewContent/XlsxPreview/xlsxDraftCache.ts:6-55` caps the cache at 50 workbooks, but each entry contains cloned current and original rows for all sheets. Reads/writes clone again.                              | Add a global byte budget and per-workbook limit. Avoid unnecessary deep clones.                                                                        | Entry count does not bound RAM for large workbooks.                                                                                            |
| Terminal idle processing            | **fix — multiplied CPU**                   | `src-tauri/crates/terminal/src/agent_tool/mod.rs:411-712` creates a child-reaper thread polling every 200 ms for each PTY. Idle readers back off through 1/5/16 ms to 50 ms and then continue indefinitely.                                                                              | Prefer blocking process/PTY notification, consolidate child reaping, and suspend readers for inactive terminals.                                       | Wakeups and threads multiply with terminal count; the frontend permits up to 20 terminal sessions.                                             |
| Terminal retained output            | **fix — high RAM bound**                   | `src/components/TerminalInteractive/terminalOutputSchedulerConstants.ts:20` allows a 2 MB hidden backlog per pane. `src/components/TerminalInteractive/bufferCache.ts:30` caps entries but not serialized-buffer bytes.                                                                  | Add a shared terminal byte budget and truncate in-memory serialized buffers, not only persisted copies.                                                | Twenty panes can retain roughly 40 MB of backlog before string and object overhead.                                                            |
| Automation file watchers            | **fix — unbounded queue and idle wakeups** | `src-tauri/crates/agent-core/src/integrations/automation/triggers/file_watch.rs` uses an unbounded channel and checks it every 200 ms per enabled rule. Events continue queuing during debounce processing.                                                                              | Use a bounded/coalescing queue and a blocking receive with a deadline. Define overflow behavior.                                                       | Idle wakeups multiply by rule count; event storms can grow the queue without limit.                                                            |
| Duplicate diagnostics scheduling    | **fix — duplicate ownership**              | `src/diagnostics/useDiagnosticsBootstrap.ts:152` schedules frontend snapshots and flushes. `src-tauri/src/usage_diagnostics/service.rs:207-225` independently starts a backend scheduler at the same configured cadence.                                                                 | Give one layer ownership and invoke the other collection as part of the same pass.                                                                     | Two recurring queues can collect system data and flush independently.                                                                          |
| Narrow-chat DOM lookup              | **fix — indefinite polling**               | `src/engines/ChatPanel/hooks/useNarrowChatFocus.ts:81-243` retries DOM selectors every 120 ms without a maximum attempt count.                                                                                                                                                           | Use a scoped `MutationObserver` or stop after a deadline.                                                                                              | If the expected elements never mount, the timer can wake for the remainder of the app session.                                                 |
| Provenance hook inbox               | **fix — unbounded spool scan**             | `src-tauri/src/orgtrack/session_provenance/hook_capture.rs:23-283` runs every 15 seconds, collects and sorts all JSON filenames, then processes no more than 1,000.                                                                                                                      | Apply directory/spool quotas, bounded enumeration, and producer backpressure.                                                                          | The on-disk spool and filename vector can grow if producers exceed 1,000 records per 15 seconds or ingestion repeatedly fails.                 |

## App-lifetime recurring process inventory

| Area                            | Verdict                     | Evidence                                                                                                                                                                                                                                                     | Change or reason kept                                                                                                             | Verification                                                                                               |
| ------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Data-source automatic scanning  | **fix/monitor**             | `src/store/session/useDataSourceAutoScan.ts` owns an app-wide recursive timer that wakes every 30 seconds. Configured source scans can run from 60 seconds upward; unfocused full scans are limited to once per 10 minutes and absence probes to 30 minutes. | Schedule directly for the next due source instead of waking every 30 seconds. Preserve single-flight and focus catch-up behavior. | Full scans can import external histories and reload the complete sidebar session cache.                    |
| Org2Cloud synchronization       | **keep, potentially heavy** | `src/features/Org2Cloud/org2CloudSyncLifecycle.ts` runs one serialized pass every 60 seconds while visible and 5 minutes while hidden. Event/retry delays range from 1.5 to approximately 30 seconds.                                                        | Current single-flight, hidden throttling, and stop lifecycle are sound. Consider dirty-entity queues for large organizations.     | Work can scan cloud organizations and local session candidates and perform network serialization.          |
| Guest-share access validation   | **keep/monitor**            | `src/features/Org2Cloud/useOrg2CloudGuestShareAccess.ts` validates the active capability every 5 seconds and all retained capabilities every 60 seconds. It stops while hidden and on unmount.                                                               | Keep current lifecycle; prefer push revocation/invalidation if capability count grows.                                            | Network cost scales with retained guest-share capabilities.                                                |
| Service authentication          | **keep**                    | `src/hooks/auth/useServiceAuth.ts:57-326` performs a visible-only expiry check every 30 seconds with single-flight protection and immediate visible-state revalidation.                                                                                      | Lightweight validity check with correct lifecycle.                                                                                | Stops hidden and cleans up on auth lifecycle change.                                                       |
| Git auto-fetch                  | **fix/monitor**             | `src/hooks/git/useGitAutoFetch.ts` can run `git fetch --prune` at a configurable cadence with a 30-second minimum, followed by a forced status refresh.                                                                                                      | Pause while hidden and use adaptive backoff for repeated no-change/error outcomes.                                                | Network and Git subprocess work continues for the active repository.                                       |
| Git source-control watcher      | **keep with cleanup fix**   | `src-tauri/crates/git/src/watch/watcher.rs` polls at 5 seconds when source control is visible/focused, 10 seconds while focused, and 30 seconds while unfocused; failures back off to 60 seconds. `.git` events use a bounded 1,000-record channel.          | Focus-aware cadence and bounded events are reasonable. Remove all repository metadata during unwatch.                             | `last_poll_attempt` and `last_git_change` maps can retain entries across repository churn.                 |
| Git watcher health monitor      | **fix/monitor**             | `src-tauri/crates/git/src/watch/mod.rs:55-122` starts after 30 seconds and runs every 60 seconds, cloning repository state and restarting failed watchers.                                                                                                   | Avoid cloning full `GitStatus` file lists merely to check health.                                                                 | Cost grows with watched repositories and status size.                                                      |
| Routine scheduler               | **keep/monitor**            | `src-tauri/crates/agent-core/src/core/coordination/routine_scheduler.rs` checks enabled routines every 30 seconds using offloaded blocking work.                                                                                                             | Query by next-due time if routine volume becomes material.                                                                        | Ambient DB/disk work is bounded by enabled routine count.                                                  |
| Agent-organization watchdog     | **keep/monitor**            | `src-tauri/crates/agent-core/src/core/coordination/agent_org_watchdog.rs:43-586` runs immediately and every 60 seconds, loading running runs with an unbounded result limit and performing cleanup/recovery.                                                 | Index/query only watchdog-due runs and bound each recovery pass.                                                                  | Cost grows with running/stale runs; blocking work is offloaded.                                            |
| Agent-session cleanup           | **fix — lock contention**   | `src-tauri/crates/agent-core/src/state/unified.rs:22-246` scans all sessions every 60 seconds and awaits per-session idle checks while holding the outer sessions mutex.                                                                                     | Snapshot session handles, release the outer lock, then await.                                                                     | Active operations can contend with a minute-level full session scan. Singleton sessions are never evicted. |
| Housekeeper MiniCPM compaction  | **keep only as opt-in**     | `src-tauri/crates/agent-core/src/core/session/housekeeper_compaction/worker.rs:17-359` starts after 20 seconds and wakes every 45 seconds, processing at most one eligible idle session.                                                                     | Do not start the recurring loop when globally opted out. Preserve one-session-per-pass and cancellation.                          | Local model inference is CPU/GPU/RAM intensive when eligible.                                              |
| Memory consolidation            | **fix/monitor**             | `src-tauri/crates/agent-core/src/core/session/specialization/memory/consolidation/tick.rs:48` owns a dedicated current-thread runtime and checks the database every 60 seconds. Triggered work can drain all pending agent scopes.                           | Wake from durable pending-work notification or track the next due time. Bound scopes per pass.                                    | Idle database scanning occurs each minute; triggered model/database work may be large.                     |
| Project synchronization         | **keep**                    | `src-tauri/crates/project-management/src/sync/worker.rs` serially pushes/merges/GCs every 30 seconds, imports one page every 60 seconds, and pulls projects every 5 minutes. Push, merge, and GC batches are capped.                                         | Durable synchronization is a justified background exception. Retain bounds and backoff.                                           | Persistent disk/network work, but per-tick processing is limited and blocking work is generally offloaded. |
| Codex transcript reconciliation | **keep**                    | `src-tauri/src/orgtrack/session_provenance/historical_backfill.rs:744` runs every 30 seconds and considers at most 12 recent transcripts.                                                                                                                    | Incremental fingerprints and a small recent limit make this bounded.                                                              | Each pass can still stat or parse several transcript files.                                                |
| Worktree cleanup                | **keep**                    | `src-tauri/src/setup/worktree.rs:3-78` runs immediately and approximately every 6 hours, enumerating sessions/repos and running worktree prune.                                                                                                              | Low cadence is appropriate. Retain deduplication and per-repository error isolation.                                              | CPU/process intensive only during its infrequent pass.                                                     |
| Settings watcher                | **keep**                    | `src-tauri/crates/settings/src/watcher.rs:98` is event-driven but uses a 2-second timeout to observe shutdown and a 500 ms debounce.                                                                                                                         | Appropriate for a watcher thread; avoid adding additional polling layers.                                                         | Low idle cost compared with filesystem scanning.                                                           |
| Managed CLI proxy               | **keep**                    | `src-tauri/src/cli_managed_proxy.rs:104` retries only when its server exits/errors, with exponential delay capped at 30 seconds.                                                                                                                             | Bounded failure recovery is appropriate.                                                                                          | Idle server is event-driven; retry work appears only after failure.                                        |
| Window-scoped state registry    | **fix/monitor**             | `src/util/core/state/windowScopedState.tsx:331` reads and rewrites the complete localStorage registry every 30 seconds.                                                                                                                                      | Pause hidden or update only changed records. Prune stale windows aggressively.                                                    | App/window-lifetime serialization and storage writes.                                                      |
| Terminal buffer persistence     | **keep/monitor**            | `src/services/terminal/bufferPersistence.ts` checks pending buffers every 30 seconds. Persisted data is limited to 10 buffers of 500,000 characters.                                                                                                         | No-pending work is cheap. Coordinate persisted and in-memory data under one byte budget.                                          | Timer is app-lifetime; pending records are cleared before write.                                           |
| Realtime WebSocket keepalive    | **keep**                    | `src/api/realtime/websocket/client.ts:349` sends a ping every 30 seconds while connected and uses bounded reconnection behavior.                                                                                                                             | Required protocol keepalive.                                                                                                      | Debug log is capped at 500 entries.                                                                        |
| Application updater             | **keep**                    | `src/scaffold/AppUpdater/appUpdaterScheduler.ts` checks after 10 seconds and then every 2 hours. Focus/online/visibility triggers are debounced and enforce a five-minute minimum.                                                                           | Low cadence and trigger floor are appropriate.                                                                                    | The timer persists while hidden, but expensive work is infrequent.                                         |
| Kanban current-time clock       | **keep**                    | `src/features/TaskKanban/hooks/useKanbanNowTick.ts` updates every 30 seconds while visible and stops while hidden.                                                                                                                                           | Correct visibility-aware clock lifecycle.                                                                                         | One clock per mounted hook.                                                                                |

## Feature- and session-scoped polling

| Area                                       | Verdict                                    | Evidence                                                                                                                                                                                                                                                      | Change or reason kept                                                                                                   | Verification                                                                                      |
| ------------------------------------------ | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| External-history active-session refresh    | **fix/monitor**                            | `src/engines/SessionCore/sync/externalHistoryAutoRefresh.ts` polls the active imported session at a configured 3-second to 1-minute cadence. Unfocused work is throttled to 60 seconds, but the underlying timer can continue waking at the original cadence. | Reschedule at the effective cadence and stop while the document is hidden.                                              | Cheap signature checks can lead to complete history load/parse/merge when changed.                |
| Native event-cache synchronization         | **fix**                                    | `src/engines/SessionCore/sync/sessionSyncLifecycle.ts` checks every 30 seconds and serializes the complete EventStore to SQLite when its version is dirty.                                                                                                    | Persist deltas/checkpoints instead of the complete store. Pause hidden if durability requirements permit.               | Serialization and IPC grow with session history.                                                  |
| Browser URL polling                        | **fix/monitor**                            | `src/hooks/platform/useInlineWebview/useWebviewUrlPolling.ts` begins after 500 ms and calls `get_webview_url` every 1 second while a webview is created and logically visible.                                                                                | Prefer navigation events and add non-overlap protection.                                                                | Cleans up on hide/unmount but is still time-driven.                                               |
| Browser console/network diagnostics        | **keep — development only**                | `src/hooks/workStation/browser/browserDiagnosticsPolicy.ts` disables log polling in production. Development pollers are visibility-aware at approximately 1 second.                                                                                           | Retain the production gate and bounded caches.                                                                          | Console cache is capped at 10 sessions × 500 records; network cache at 10 × 200.                  |
| Webview inspector                          | **fix/monitor**                            | `src/hooks/workStation/browser/useWebviewInspector.ts:248` polls around every 300 ms while inspect mode is active.                                                                                                                                            | Stop when the document is hidden and prefer browser-side hover/selection events.                                        | Repeated webview evaluation and IPC; no overlap/visibility guarantee was found.                   |
| Browser DOM tree                           | **fix/monitor**                            | `src/hooks/workStation/browser/useWebviewDOMTree.ts:318` polls a dirty flag and can return complete DOM-tree JSON, commonly around a 1.5-second cadence.                                                                                                      | Push invalidation and request tree segments on demand.                                                                  | In-flight guard is present, but full DOM materialization can be CPU/RAM intensive.                |
| Embedded-webview visibility                | **fix/monitor**                            | `src/hooks/platform/useInlineWebview/useEmbeddedWebview.ts:229` checks container visibility every 500 ms and opens/closes native webviews.                                                                                                                    | Use observers/events and stop while the document is hidden.                                                             | Scoped to mounted embedded webviews.                                                              |
| Webview position synchronization           | **fix/monitor**                            | `src/hooks/platform/useInlineWebview/useWebviewPositionSync.ts` can measure layout approximately every 200 ms while active.                                                                                                                                   | Use observer-driven invalidation or animation-frame coalescing only after relevant layout changes.                      | Repeated `getBoundingClientRect`-style reads can cause layout work.                               |
| Workspace port scanner                     | **keep**                                   | `WorkspacePortScanner.tsx:64` scans every 60 seconds only while the editor host is enabled and the document is visible.                                                                                                                                       | Visibility lifecycle is correct. Consider event-driven server registration where possible.                              | Each tick spawns and reads an `lsof`/`netstat`-style system command.                              |
| Agent-organization run fallback            | **keep**                                   | `agentOrgRunViewStore.ts` uses push as the primary mechanism and a 60-second fallback only while subscribers exist. It is single-flight and skips terminal runs.                                                                                              | Appropriate push-plus-safety-TTL pattern.                                                                               | Timers/maps are cleaned on final unsubscribe; hidden callbacks skip work.                         |
| Cloud roster fallback                      | **keep**                                   | `useCloudOrgPanelState.ts` uses a 5-minute fallback only while signed in and realtime is disconnected.                                                                                                                                                        | Appropriate low-frequency recovery path.                                                                                | Hidden callbacks skip work and visibility return triggers refresh.                                |
| Gateway/configuration status               | **fix/monitor**                            | `useOSAgentGateway.ts` polls every 10 seconds while mounted; `CliConfigSwitchCard.tsx:170` every 3 seconds until proxy readiness; `MiniCpmCompactCard.tsx:49` every 5 seconds while mounted.                                                                  | Add document-hidden pauses, non-overlap, and terminal deadlines.                                                        | Scoped to mounted settings/configuration surfaces.                                                |
| LSP server log viewer                      | **fix/monitor**                            | `useLspServerLog.ts` fetches approximately every 1.5 seconds while the drawer/language is active. The backend log ring is bounded to 500 lines of at most 2 KB each.                                                                                          | Fetch only records after a sequence cursor rather than cloning the complete ring. Stop hidden.                          | Bounded retention, but repeated allocation/IPC of up to 500 lines.                                |
| Session-provenance activation verification | **keep/monitor**                           | Activation UI polls every 2 seconds only while awaiting verification.                                                                                                                                                                                         | Add hidden pause, but retain the explicit terminal state.                                                               | Stops after verification or unmount.                                                              |
| MCP server connection status               | **keep/monitor**                           | `src/hooks/useMcpServers.ts:163` recursively polls every 2 seconds only while at least one server is connecting.                                                                                                                                              | Add hidden pause and a maximum connection deadline.                                                                     | Non-overlapping and conditionally stopped.                                                        |
| Shared application-memory snapshot         | **fix/monitor**                            | `src/hooks/perf/appMemorySnapshot.ts` samples the process tree every 15 seconds while consumers exist. Callbacks can skip hidden work, but the shared interval continues waking.                                                                              | Stop the shared timer while hidden and run once on visibility return.                                                   | Process-tree discovery can be CPU/process intensive.                                              |
| Sidebar/settings memory monitors           | **fix/keep**                               | Sidebar monitoring runs cheap refresh at 15 seconds and expensive refresh at 60 seconds while open, potentially duplicating a cheap call on the 60-second tick. Settings monitoring correctly stops when hidden or offscreen.                                 | Deduplicate the sidebar cadence; retain settings intersection/visibility gating.                                        | Feature-scoped but invokes process and system metrics.                                            |
| File-watch heartbeat                       | **fix/monitor**                            | `useFileWatchHeartbeat.ts` wakes every 30 seconds once explicitly started and appends an output message every minute after one minute of inactivity.                                                                                                          | Bound/log-coalesce heartbeat output and stop when its owning operation ends.                                            | Output history can grow for a long-lived idle watcher.                                            |
| Benchmarks and active runners              | **keep**                                   | Status polling commonly runs every 2 seconds only during an active benchmark/batch.                                                                                                                                                                           | Explicitly user-triggered and terminal-state bounded.                                                                   | No recurring work after completion/cancellation.                                                  |
| Wingman observation loop                   | **keep only as explicit high-cost opt-in** | `src-tauri/crates/agent-core/src/core/session/wingman/loop_runner.rs` captures a screenshot, gathers activity, and initiates an LLM observation every 30 seconds during an active Wingman session. Native UI also has a 1-second elapsed-time clock.          | Retain singleton ownership, `CancellationToken`, and explicit user activation. Consider adaptive observation frequency. | CPU, screenshot allocation, network, and model-token intensive by design.                         |
| Automation interval/scheduled rules        | **keep except file-watch implementation**  | Each enabled interval rule owns a sleeping task; a scheduled occurrence uses one long sleep. Engine health broadcasts every 30 seconds.                                                                                                                       | Lifecycle and abort behavior are reasonable. Avoid one wakeup thread/task per rule at very large rule counts.           | Work scales with enabled rules; file-watch queue issue is documented separately.                  |
| Gateway inbound merge drain                | **fix/monitor**                            | `src-tauri/crates/agent-core/src/integrations/gateway/workers.rs:28-50` checks the merge buffer every 50 ms while the gateway runs. The merge window is approximately 600 ms and has no count/byte cap.                                                       | Drive draining from the next deadline or incoming events and cap bytes/messages per sender and globally.                | Idle wakeups are persistent while enabled; short bursts can create unbounded transient retention. |
| Telegram polling                           | **keep**                                   | Uses HTTP long-polling with an approximately 30-second server timeout and a 5-second error delay.                                                                                                                                                             | Standard long-poll lifecycle when the channel is enabled.                                                               | Stops by aborting the channel worker.                                                             |
| Weixin polling                             | **keep**                                   | Uses approximately 25-second long polls, a short request timeout, and bounded 2–10-second error backoff. Context/dedup caches are capped at 1,000.                                                                                                            | Appropriate enabled-channel behavior.                                                                                   | Bounded caches and retry cadence.                                                                 |
| Feishu WebSocket heartbeat/fragments       | **fix memory lifecycle**                   | Heartbeat defaults around 120 seconds. Fragment records have a five-minute TTL, but pruning is tied to connection setup instead of continuous stable-connection operation.                                                                                    | Prune on insertion/interval and add count/byte caps.                                                                    | Incomplete fragment IDs can accumulate until reconnect.                                           |
| WeCom heartbeat                            | **keep**                                   | Heartbeat runs every 30 seconds with a 90-second read timeout and bounded reconnection.                                                                                                                                                                       | Appropriate protocol lifecycle.                                                                                         | Heartbeat task is aborted when the connection ends; reply IDs are capped at 1,000.                |
| Discord heartbeat                          | **fix — duplicate task risk**              | Heartbeat follows the server interval, commonly approximately 41 seconds. Each Hello can spawn a new heartbeat task without retaining/aborting the previous handle.                                                                                           | Enforce exactly one heartbeat owner per connection generation.                                                          | Repeated Hello frames on the same connection can create overlapping tasks.                        |
| Active LLM/PTY/process loops               | **keep**                                   | Cursor request heartbeat is approximately 5 seconds. PTY output/status/cancellation loops commonly use 25–50 ms. Provider cancellation checks use 50 ms.                                                                                                      | Request/command-scoped polling with explicit completion/cancellation is acceptable.                                     | No ambient work should remain after the operation terminates.                                     |
| Turn-index debounce                        | **fix/monitor**                            | `src-tauri/crates/session-persistence/src/turn_index_debounce.rs` creates one OS thread per streaming session, checks quiet state every 250 ms, then rebuilds the turn index.                                                                                 | Use a shared scheduler and incremental index maintenance.                                                               | Continuous streaming creates four wakeups per second per session before the final full rebuild.   |

## Retained-memory and cache inventory

| Area                               | Verdict                                  | Evidence                                                                                                                                                                                                     | Change or reason kept                                                                                            | Verification                                                                                                 |
| ---------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| Backend EventStore session cache   | **fix/monitor**                          | `src-tauri/src/agent_sessions/event_pipeline/session_manager.rs` targets 15 idle and 25 total sessions, but pinned sessions cannot be evicted. Each retained EventStore can contain complete history.        | Add a pinned-session byte budget and spill inactive history to persistence.                                      | Total count can exceed the target when all candidates are pinned/non-idle.                                   |
| Frontend snapshot cache            | **fix/monitor**                          | `src/engines/SessionCore/core/store/snapshotCacheManager.ts` targets five snapshots and a 15,000-event budget. `snapshotCache.ts:55` preserves the newest/only snapshot even if it alone exceeds the budget. | Add a hard byte cap and permit dematerializing an oversize newest snapshot.                                      | Normalized and materialized forms duplicate graph data; weight does not cover every index/simulator payload. |
| Approval-detail cache              | **fix — unbounded**                      | `src/engines/ChatPanel/InputArea/components/useAgentOrgPlanApprovalDetail.ts:27` retains complete detail/error data for every approval revision without eviction.                                            | Apply LRU/TTL/byte limits and remove entries on plan/session disposal.                                           | Listener cleanup does not evict module cache entries.                                                        |
| Syntax-highlight cache             | **fix — byte-unbounded**                 | `src/features/CodeViewer/utils/syntaxHighlight.ts:47` caps 5,000 entries, but keys contain source lines and values contain generated HTML.                                                                   | Use byte-aware LRU and maximum source-line/output sizes.                                                         | Count bounds do not bound memory for long source lines.                                                      |
| Repository file-search indexes     | **fix/monitor**                          | `src-tauri/crates/search/src/file.rs:66` keeps up to four full repository indexes for five minutes. Cache reads clone the complete `Vec<FileEntry>`.                                                         | Share immutable indexes and add global/per-index byte budgets.                                                   | Large monorepos can create substantial resident and transient duplicate memory.                              |
| Navigation history                 | **fix — unbounded**                      | `src/services/navigation/NavigationService.ts` keeps a module-level history array without a size cap.                                                                                                        | Use a small ring buffer.                                                                                         | Grows with navigation count for the app lifetime.                                                            |
| LocalStorage mirror cache          | **fix/monitor**                          | `src/util/core/storage/localStorage.ts` stores every accessed key/value in a module `Map` without eviction.                                                                                                  | Avoid mirroring large values or add LRU/TTL limits.                                                              | Usually low-cardinality, but formally unbounded.                                                             |
| Work-item blocked-notification map | **fix — unbounded**                      | `work_item_scheduler.rs:24` stores last-sent times by work-item/reason and never prunes them.                                                                                                                | Remove entries on terminal/deletion and run TTL cleanup.                                                         | Accumulates across historical work items and changing block reasons.                                         |
| Git watcher metadata maps          | **fix — unbounded churn**                | Per-repository last-poll and last-change maps are not removed by `unwatch_repo`.                                                                                                                             | Remove all per-repository metadata during teardown.                                                              | Small entries, but no lifetime bound across repository churn.                                                |
| Agent live-status maps             | **fix/monitor**                          | `src-tauri/src/orgtrack/agent_live_status.rs:74-165` uses a 30-minute TTL but physically removes expired data only during a later ingest.                                                                    | Clean on run completion or from a bounded periodic expiry queue.                                                 | A final event burst can remain resident indefinitely if no later event arrives.                              |
| Gateway merge buffer               | **fix — burst-unbounded**                | Incoming messages are retained for an approximately 600 ms merge window without a count/byte limit.                                                                                                          | Add sender/global byte caps and explicit overflow/coalescing behavior.                                           | Short-lived under normal load, unbounded during a burst.                                                     |
| Feishu fragment cache              | **fix — unbounded on stable connection** | Incomplete fragments have nominal TTL but no continuous pruning/count cap.                                                                                                                                   | Prune on insert and deadline; cap total fragment bytes and IDs.                                                  | Can grow until the connection restarts.                                                                      |
| Streaming delta accumulator        | **fix/monitor**                          | Per-session streaming state retains the full accumulated response and clones the containing map during UI publication.                                                                                       | Use chunk/rope storage, narrower per-session subscriptions, and guaranteed cleanup on every terminal/error path. | Large responses can be duplicated; missed terminal events may retain state.                                  |
| Browser screenshot caches          | **keep/monitor**                         | Frontend cache is approximately 20 entries/24 MB; backend cache approximately 20 entries/32 MB.                                                                                                              | Explicit limits are good. A cross-layer global budget would better reflect decoded-image cost.                   | More than 50 MB can be retained before decoded bitmap/object overhead.                                       |
| Browser console/network caches     | **keep**                                 | Console retention is capped at 10 sessions × 500 entries with message truncation; network retention at 10 × 200.                                                                                             | Bounded and development polling is production-disabled.                                                          | Reads can still clone arrays at each poll.                                                                   |
| LSP logs                           | **keep/monitor**                         | Backend retains up to 500 lines of 2 KB per server.                                                                                                                                                          | Bound number of retained server rings and fetch by cursor.                                                       | Per-server ring is bounded; total grows with server count if server-state eviction is absent.                |
| Loaded historical turn bodies      | **keep**                                 | Historical session loading keeps a small recent window, approximately eight bodies per session, with payload caps around 6–8 MB.                                                                             | Demand-driven and bounded.                                                                                       | Verify eviction on session deletion and identity switch.                                                     |
| Rendering caches                   | **keep**                                 | Shiki, Mermaid, PR-file, and screenshot-related caches have entry and/or byte limits.                                                                                                                        | Existing bounded LRU patterns are preferable to count-only caches elsewhere.                                     | Validate eviction tests when these caches change.                                                            |
| API diagnostics tracker            | **keep/monitor**                         | API calls, timer events, and push events have bounded history counts, but request timing state may retain requests that never complete.                                                                      | Expire or cancel timing entries for abandoned requests.                                                          | History is bounded; in-flight request IDs need a terminal timeout.                                           |

## Low-cost repeated UI clocks and animations

The following processes are individually low-cost and generally component-scoped with cleanup, but many simultaneous instances can still create avoidable wakeups:

- One-second countdowns and elapsed-time displays in plans, questions, modes, process lists, title adapters, MCP rows, open-source countdowns, and download progress.
- Voice-recording elapsed time at approximately 250 ms.
- Streaming status HUD updates at approximately 500 ms.
- Kanban, replay, and simulator autoplay timers based on playback speed.
- Loading/stroke animations around 2.2 seconds.
- Development-only streaming simulations and API-call panel polling.

Shared clocks should be preferred where many copies can mount simultaneously. Nonessential clocks and animation work should stop when hidden.

## On-demand CPU/RAM-intensive operations

These are not ambient pollers but can cause significant bursts and should remain demand-driven and bounded:

| Area                                        | Verdict         | Evidence                                                                                                                                 | Change or reason kept                                                                    | Verification                                                       |
| ------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| File/code search                            | **monitor**     | Repository walks build full file vectors; text search can collect and process many files in parallel; cached file indexes may be cloned. | Paginate results, share indexes, cancel stale searches, and cap concurrency/memory.      | Profile on a large monorepo.                                       |
| Semantic indexing/embedding                 | **monitor**     | Tokenization, batching, Metal/GPU contexts, and recovery paths can consume CPU/GPU/RAM.                                                  | Explicit user initiation, cancellation, batch caps, and resource release are required.   | Measure peak RSS/GPU memory and post-cancel release.               |
| External-history import/reconciliation      | **monitor**     | Full transcript files may be parsed, merged, fingerprinted, and used to rebuild sidebar/session state.                                   | Incremental fingerprints, due-source scheduling, and pagination.                         | Measure large history sets and focus-return behavior.              |
| Git operations                              | **monitor**     | Fetch, status, diff, log, remote, and worktree commands spawn processes and can traverse large repositories.                             | Coalesce invalidations, share equivalent calls, and cancel stale work.                   | Measure large dirty repositories and repeated focus changes.       |
| Browser inspection/capture                  | **monitor**     | DOM serialization, screenshots, inspector evaluation, console/network transfer, and native webview coordination cross IPC boundaries.    | Load only visible/expanded data and enforce byte limits.                                 | Measure a large DOM and repeated inspector open/close cycles.      |
| Spreadsheet rendering/editing               | **fix/monitor** | CSV parsing and XLSX sheet paging clone large nested arrays, with draft caches retaining original and edited copies.                     | Virtualize, page, avoid deep clones, and enforce byte budgets.                           | Measure large CSV/XLSX peak and post-close RAM.                    |
| Syntax/diagram rendering                    | **monitor**     | Shiki/highlight.js and Mermaid generate formatted strings/DOM from source content.                                                       | Cache by bytes, cancel stale renders, and render visible content only.                   | Measure very large files and diagrams.                             |
| System diagnostics                          | **monitor**     | Process-tree discovery, system profiles, and aggregation across sessions/workspaces can spawn processes and allocate result graphs.      | Single scheduler, shared snapshots, and visibility-aware demand.                         | Measure collection duration and allocation on large process trees. |
| Local model and LLM workflows               | **monitor**     | MiniCPM compaction, memory consolidation, Wingman screenshots, and LLM observations are intentionally compute/network intensive.         | Explicit activation, strict singleton ownership, cancellation, and bounded queues.       | Measure active and post-cancel CPU/GPU/RAM.                        |
| Tests, Docker, benchmarks, and PTY commands | **keep**        | Explicit user actions can create subprocess trees and high output volume.                                                                | Operation-scoped ownership, output backpressure, cancellation, and cleanup are required. | Verify process trees and buffers disappear after stop/close.       |

## Lifecycle gaps observed

| Dimension           | Main gaps                                                                                                                                                             |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| App idle            | Work-item scheduling, data-source due checks, diagnostics, session cleanup, provenance ingestion, and several service schedulers wake continuously.                   |
| Document hidden     | Git auto-fetch, native session persistence, several status/config pollers, inspector/DOM work, memory snapshots, and UI clocks lack complete hidden-state suspension. |
| Network failure     | Hosted-key retries can grow memory; channel reconnects are bounded but fragment/merge buffers need hard caps.                                                         |
| Session disposal    | Pinned EventStores, frontend snapshots, approval details, streaming accumulators, and some terminal buffers need stronger byte budgets and terminal cleanup.          |
| Feature unmount     | Most React timers clean up, but module-level caches outlive their components.                                                                                         |
| Repeated open/close | Terminal threads/buffers, browser caches, LSP server rings, and per-repository watcher metadata require stabilization tests.                                          |
| Multiple consumers  | Diagnostics has duplicate owners; memory polling and some Git/session queries can be duplicated by separate surfaces.                                                 |

## Recommended remediation order

1. Replace the 33 ms full EventStore snapshot build with incremental streaming deltas and incremental indexes.
2. Replace global work-item scans with indexed due-item queries and move blocking database work off async executor threads.
3. Replace work-item message/file/diff polling with push invalidation and cursor/delta queries.
4. Add hard byte limits and eviction to hosted-key activity, CSV/XLSX drafts, terminal output, approval details, navigation history, channel fragments, and merge buffers.
5. Consolidate terminal child/read polling and per-stream turn-index threads into shared or blocking notification mechanisms.
6. Remove duplicate diagnostics ownership and share system/memory snapshots between consumers.
7. Apply visibility-aware recursive scheduling to every noncritical frontend poller.
8. Add TTL/lifecycle pruning for blocked notifications, Git watcher metadata, agent live status, pinned session state, and server/channel caches.
9. Add performance regression tests for bounds, cleanup, non-overlap, hidden-state suspension, and repeated open/close cycles.
10. Perform runtime profiling in visible idle, hidden idle, active streaming, large-history, multi-terminal, and post-disposal scenarios.

## Verification status

Completed:

- Static searches for timers, polling loops, retries, watchers, caches, queues, and retained collections.
- Production call-path tracing for the highest-risk findings.
- Ownership, cadence, visibility, bound, and cleanup classification where identifiable from source.
- Semantic-peer sweep across frontend, backend, session, Git, browser, terminal, cloud, automation, gateway, and diagnostics paths.
- Follow-up source comparison from the audit commit through the current branch.
- Targeted regression tests for external-history visibility/focus scheduling, non-overlap, disposal, direct session-switch eviction, and stale async completion rejection.
- TypeScript typecheck and real large-Codex-session timing/RAM sampling for the lazy-load path.
- Work-item exact-deadline scheduling, shared session-event invalidation, provenance spool quotas/wakes, and bounded cache regression suites.
- Terminal, CSV/XLSX, stream-accumulator, Git watcher, repository-index, browser lifecycle, and visibility-aware scheduler regression suites.
- Real packaged Tauri CTRL+5 checks: cold idle produced no calls at 30/60 seconds; Work-item initial traffic matched database cardinality and remained flat; the only later triplet was one due external-history scan.
- Real process-tree checks: visible idle without Computer Use capture was 0.00% CPU over 20 seconds with 685.3 MiB working set; minimized idle was 0.00% CPU with 680.7 MiB working set.
- Final Computer Use checks on the follow-up build: a large Codex session rendered its shell/skeleton in 654 ms; the stable empty external session showed no animated selector and used 1.89% single-core CPU over 20 seconds; a minimized large-session process tree used 0.31% over 20 seconds.
- Final hidden-lifecycle CTRL+5 check: no external-history or cloud calls were recorded while minimized. The calls visible immediately after restore were local Store reads caused by hovering the diagnostics surface itself.
- Focused TypeScript regression suites passed 17 tests across three files. Typecheck, focused ESLint, Prettier, `git diff --check`, and the final Tauri development build completed successfully.

Not completed:

- Multi-terminal and repeated open/close stabilization measurements at the 20-pane limit.
- Large-session, large-workbook, large-repository, and event-storm stress tests.
- Persistence-backed paging/dematerialization for a single oversize active EventStore/snapshot; current count/event limits and external-history pagination bound normal use, but this remains the principal load-scale monitor.

Performance verdict: pass for the remediated production paths in this follow-up; conditional at product-wide load scale for the three explicit stress-test monitors above.
