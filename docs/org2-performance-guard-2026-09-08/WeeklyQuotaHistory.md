# Weekly quota history performance guard — follow-up audit

| Area                            | Verdict | Evidence                                                                                                                                                     | Change or reason kept                                                                                                                                                    | Verification                                                                                                              |
| ------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| Background work                 | fix     | Sampler previously loaded up to 128 × 672 points, then invoked every eligible account on a wake; newly connected keys waited until the next timer            | Dedicated due-ID RPC, enabled/cooldown gates, unseen/oldest-first batch; account-change subscription coalesces one follow-up; one visible/online hourly timer            | Sampler makes zero history-read calls; disabled/recent/unseen-account Rust tests and fake-timer coalescing/disposal tests |
| Memory / rendering              | fix     | Before-change regression mounted 128 chart components for 128 accounts                                                                                       | One selected chart, using the existing searchable Select; 672 observations per account remain bounded; no new chart dependency or clock timer                            | Same regression now mounts exactly one chart before and after account selection; no CPU/RSS claim                         |
| Subscription scope              | fix     | Every shared-key publication invalidated history                                                                                                             | Subscribe only to account identity/display changes; sort map fields so serialization order does not trigger work                                                         | 100 quota/health publications → zero callbacks; identity and endpoint changes still invalidate                            |
| Identity / stale writes         | fix     | Global in-flight history promise could survive unmount and be joined under another identity; profile enrichment or token renewal split history               | Identity-keyed promises; shared Codex account extractor; transactional profile-scope promotion at shared quota persistence boundary; existing generation guards retained | Mounted and remounted account-switch tests; token/organization/endpoint separation; profile/cooldown promotion tests      |
| Retention / authoritative scope | fix     | Capped UI inventory was treated as the full account deletion inventory                                                                                       | Scheduler pruning uses every vault key and the 28-day TTL; display reads do not delete histories; existing keys retain older scopes until TTL                            | 130-key fixture preserves key 129 and its older scope; actual key removal prunes both                                     |
| Chart semantics                 | fix     | Exactly two hours between readings bridged a missed sample; img wrapper conflicted with chart accessibility; errors also displayed disconnected-account copy | Gaps after 90 minutes; explicit error/stale/disabled states; stable 28-day axis; native chart accessibility layer and observation dots                                   | Projection and DOM component regression tests                                                                             |

Lifecycle evidence:

- Start/visible: lightweight due lookup, sequential eligible samples; zero historical points fetched by sampler
- Visible idle: one hourly timeout, no per-frame or per-second work
- Hidden/offline: clear timer; no new account request; an existing bounded request may finish
- Return/account change: one revalidation; changes during a pass queue one non-overlapping follow-up; backend claim still enforces cooldown
- Disabled: both discovery and sampling entry reject automatic work; history remains readable
- Identity change/unmount: signatures invalidate relevant reads; generations reject old completions; remounts cannot join another identity's request
- Removal/retention: full-key inventory and time-based pruning, independent of the display cap
- Secondary instance: instance storage path and main-window guard retained; concurrent SQLite-connection claim test passes, but direct secondary launch remains unmeasured
- Session/cloud/source-ingestion dimensions: not changed; no synchronization or transcript behavior claim

Verification: **399 Key Vault Rust tests**, **22 targeted frontend tests**, TypeScript and changed-file ESLint pass. Existing Claude Code, Codex and OpenCode Go adapter fixtures are included in the Rust suite. Whole-app check and build passed in the original checkout after rebuilding stale dependency artifacts. Isolated PR verification is recorded in the PR description. The scoped diff passes.

Performance verdict: **blocked for live validation** — automated lifecycle and component/call-count evidence passes, but authenticated endpoint behavior, actual WebView interactions, CPU/RSS and direct secondary isolation were not measured. Computer control was not authorized. No measured runtime latency, memory or frame-rate improvement is claimed.

## CI promise-handling correction

| Area            | Verdict | Evidence                                                                                             | Change or reason kept                                                                                                                      | Verification                                                                                                           |
| --------------- | ------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| Background work | fix     | Type-aware CI rejected three unhandled tick promises; an error callback could itself reject the tick | All startup, timer, and wake paths use one rejection-handling launcher; diagnostics omit raw errors, and cadence/disposal remain unchanged | Scheduler and history-hook tests: 9 passed, including throwing-error-callback coverage across startup, timer, and wake |

Live UI/CPU/RSS validation remains unmeasured; this correction makes no performance improvement claim.
