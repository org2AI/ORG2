# Spotlight cleanup lifecycle verification

| Area               | Verdict | Evidence                                                                                                                                  | Change or reason kept                                                                                                 | Verification                                                                                    |
| ------------------ | ------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Background work    | fix     | Root selector previously active under child layers; focus timers survived inactivity                                                      | One active route owns keyboard handling; one cancelable timer per focus owner; pending microtasks invalidate on close | Root listener-count composition test; rapid open/close focus regression; 304-test suite passes  |
| Scope/isolation    | fix     | GitHub branch completion outlived its selected connection/view                                                                            | Effect cleanup rejects old success/error/finally; one mapper                                                          | Deferred A/B connection and close tests; real provider network not exercised                    |
| Cache correctness  | fix     | Mutation refresh reused a pre-mutation enumeration                                                                                        | Invalidate request token, start post-mutation request, guard cache writes and promise cleanup                         | Create/remove races plus old-first/new-first completion ordering covered                        |
| Memory             | keep    | Completed worktree cache retains at most 16 entries with a five-minute TTL; in-flight records cleaned in finally; subscribers unsubscribe | Preserve bounds; refreshing an existing key no longer evicts an unrelated key; no retained generation-counter map     | Source inspection of bounds/eviction/cleanup, request-coalescing tests. No heap/RSS measurement |
| Rendering/hot path | keep    | Existing shared virtualized list; command registry subscription exists only while enabled                                                 | Preserve list virtualization; supported-command snapshot is a stable string; no polling introduced                    | Existing virtualization tests and late registration/unregistration test                         |
| Root lifetime      | keep    | Portal conditionally mounts on open; standalone consumers may stay mounted                                                                | Preserve portal disposal and explicitly guard inactive standalone work                                                | Listener removal after unmount, inactive focus and branch completion tests                      |

## Lifecycle matrix

| Resource                        | Active / start                                    | Inactive / close                               | Reopen / scope change                                         | Evidence                                                              |
| ------------------------------- | ------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------------- |
| Keyboard listener               | Root or child owns capture navigation             | Root disabled under child; released on unmount | Root selection restored by item identity                      | Root composition test                                                 |
| Focus timer and reset microtask | At most one scheduled focus per owner             | Canceled and active guard cleared              | New lifecycle schedules new work                              | Rapid transition and existing focus tests                             |
| GitHub request                  | Valid connection while active                     | Obsolete completion ignored                    | A cannot overwrite B                                          | Deferred mocked requests; current OSS provider is a stub              |
| Worktree request                | Equivalent reads share in-flight request          | Still-valid cache generation may finish        | Mutation starts fresh generation; old finally cannot clear it | Deferred cache boundary tests                                         |
| Registry subscription           | Command mode subscribes to registration changes   | Disabled mode does not subscribe               | Late registered commands become selectable                    | Hook regression                                                       |
| Cache/list                      | Bounded completed cache and virtualized rendering | Subscriptions disposed on unmount              | TTL revalidation remains demand-driven                        | Source inspection and existing suite; no idle performance measurement |

Online/offline retries, authentication/endpoint switching, secondary instances, transcript ingestion and cloud transport were not changed and are not claimed as verified. No new intervals, workers or polling paths were added.

Automated lifecycle checks, full fast TypeScript, scoped ESLint/oxlint, dependency boundaries, file lengths and scoped diff whitespace checks pass. No remaining stale-write or duplicate-root-listener path from the audited findings is known.

**Performance verdict: blocked** for native measurement only. The user's explicit no-computer-control preference leaves native pointer/focus behavior, visible/hidden idle CPU/RSS and repeated real-window open/close resource measurements unexecuted. This is not a claim of measured performance improvement or a remaining implementation blocker; automated lifecycle invariants are covered as listed above.

## UI consolidation lifecycle follow-up

| Area               | Verdict | Evidence                                                                                                  | Change or reason kept                                                                       | Verification                                                       |
| ------------------ | ------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Background work    | keep    | Shared panel only composes existing engine output; no new listeners, intervals, observers or fetch owners | Pickers retain their existing engine and list lifecycle                                     | Portal unmount regression and existing picker suites               |
| Memory             | keep    | New fields/surfaces retain no collections; clone guard is one ref per mounted hook                        | No new cache or accumulating retained state                                                 | Source inspection                                                  |
| Scope/isolation    | fix     | A second clone previously could be submitted while the first was pending                                  | Synchronous in-flight guard plus actual loading state in both forms; finally releases guard | Deferred clone test verifies one action dispatch and loading reset |
| Rendering/hot path | keep    | Compact row forwards hover/ref contract; existing virtualized lists retained                              | One shared row presentation with sibling variant action                                     | Directory hover composition test and row activation test           |

The directory dialog remains delegated to existing callbacks; one field applies the chosen path. Active/open/close behavior retains the existing dropdown engine. Closing a clone form does not cancel an already-dispatched clone; cancellation semantics and cross-instance deduplication are not added by this change.

**Performance verdict: blocked** for native measurement: no explicit computer-control opt-in. Visible/hidden idle CPU/RSS, real-window reopen loops and native pointer/focus measurements remain unexecuted. Automated lifecycle and concurrency tests pass; no runtime speed or memory improvement is claimed.

## CI follow-up

Selector reset and selection scheduling now use queueMicrotask directly; existing effect cancellation guards still reject work after cleanup. No new timer, retained state or resource owner is introduced. Navigation and directory callback promises now handle rejection explicitly. Automated lifecycle coverage is rerun; the native measurement limitation and performance verdict remain unchanged.
