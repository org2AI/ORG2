# Spotlight second-pass lifecycle audit

Status: this is a historical second-pass audit. The subsequent UI consolidation implements clone busy-state propagation, duplicate-submit protection and removal of the unreachable combined clone form. Other findings remain follow-up work; see the cleanup/component reports for delivered scope.

| Area               | Verdict | Evidence                                                                                                  | Change or reason kept                                                                  | Verification                                                                        |
| ------------------ | ------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Scope/isolation    | fix     | File search commits after query/root/lifetime changes                                                     | Generation/lifetime guard for success/error/loading writes                             | Deferred A/B probe reproduced old query replacing new result                        |
| Scope/isolation    | fix     | Symbol cache not scoped in visible projection; cleanup only cancels debounce                              | Scope results by file and invalidate generation at cleanup                             | A rows actionable under B; closed result suppressed fresh reopen fetch              |
| Cache correctness  | fix     | Branch cache/loading identity omits checkout path; empty successful entry not projected                   | Shared scoped key across Spotlight and SessionCreator; represent successful empty data | Distinct-path overwrite, fresh-cache wrong-path reuse, empty-cache/stale-row probes |
| Background work    | fix     | Refresh wrapper spawns one stop timeout per click with no teardown                                        | One owned timer/deadline and defined overlapping-request semantics                     | Earlier timer stopped spin while second refresh was pending                         |
| Background work    | fix     | Shell refocus uses unowned zero-delay timeout                                                             | Coalesce and dispose queued focus; sweep alongside refresh owner                       | Source only; live-ref lookup normally prevents post-unmount focus                   |
| Mutation ownership | fix     | Clone's busy value is discarded; callers supply false                                                     | Guard operation and expose busy state through forms                                    | Source-confirmed; no real clone executed                                            |
| Memory             | keep    | Existing worktree completed-cache count/TTL preserved; search result caps and virtualized lists preserved | No claim of new growth defect in these structures                                      | Source inspection; heap/CPU/RSS not measured                                        |
| Request coalescing | keep    | Git API already deduplicates repo/path-equivalent requests                                                | Preserve transport dedup; do not infer duplicate HTTP from duplicate hook calls        | Two existing API tests passed                                                       |

## Lifecycle matrix

| Resource         | Start/active                                  | Close/inactive                                   | Reopen/scope change                                                | Result                                           |
| ---------------- | --------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------ |
| File search      | First immediate, later debounced              | Started promise remains able to write            | Older query can win                                                | Reproduced fail                                  |
| Symbol search    | Debounced, cached by ref                      | Started completion can prime cache after disable | Wrong file rows remain actionable; same-file reopen can skip fetch | Reproduced fail                                  |
| Branch store     | API path scope collapses into repo-only cache | Global cache retained intentionally              | Checkout path can reuse another checkout's state                   | Reproduced fail                                  |
| Refresh feedback | Each trigger schedules completion work        | No timer disposal                                | Earlier timer wins over newer pending operation                    | Reproduced fail                                  |
| Clone operation  | Busy setter exists                            | No exposed ownership/guard                       | Repeated submit can enter handler                                  | Source-confirmed defect, no native mutation test |

Seven diagnostic probes plus two existing API tests passed (details in the architecture report). Diagnostic tests assert the current defects and are not a green correctness verdict. Provider transcript ingestion, auth/endpoint switching, multi-instance topology and cloud transport were not changed or exercised. No native CPU/RSS/focus measurement, real clone or network requests.

**Performance verdict: fail** — additional stale-result and competing-timer paths remain in the audited code. This verdict is based on source and React-hook lifecycle evidence, not a measured CPU/RAM regression.
