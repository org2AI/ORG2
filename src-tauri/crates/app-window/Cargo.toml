[package]
name = "app_window"
version = "0.1.0"
edition = "2021"
rust-version = "1.85.0"

# Centralised native-window helpers: macOS traffic-light positioning and
# native menu vibrancy,
# Windows rounded-corner DWM tweaks, and the "Recreate main window" flow used
# by the Tauri menu and tray.
#
# Lives in its own crate so leaf crates (currently `browser`) can call
# `app_window::set_traffic_light_position` / `apply_host_desktop_window_chrome`
# without depending on the rest of `app`. `app::lib.rs` and the macOS app
# menu still consume the same API directly.
#
# No runtime hooks, no async runtime — everything is synchronous Tauri
# operations against a `tauri::AppHandle` / `WebviewWindow`.

[lib]
name = "app_window"

[dependencies]
perf_utils = { path = "../perf-utils" }
# The startup backdrop follows the user's `general.theme`, which lives in
# `~/.orgii/settings.jsonc`. `settings` is itself a leaf crate (app_paths +
# tauri), so this stays acyclic and does not pull in `app`.
settings = { path = "../settings" }

tauri = { workspace = true }
tracing = "0.1"

# macOS-specific native-window plumbing
[target.'cfg(target_os = "macos")'.dependencies]
# Modern Objective-C bindings used by native window chrome and materials.
objc2 = { workspace = true }
objc2-foundation = { workspace = true }
objc2-app-kit = { workspace = true }
dispatch2 = { workspace = true }

# Windows-specific DWM rounded corners + acrylic backdrop + OS version
# detection (Win10 vs Win11 chrome policy in `windows_corner.rs`).
[target.'cfg(windows)'.dependencies]
windows = { version = "0.61", features = ["Win32_Foundation", "Win32_Graphics_Dwm"] }
windows-version = "0.1"
window-vibrancy = "0.6"
