[package]
name = "orgtrack_core"
version = "0.1.0"
edition = "2021"
rust-version = "1.85.0"
description = "Publishable core for developer activity collection, canonical orgtrack records, SQLite storage, replay/stat projections, and repo sync."
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "orgtrack_core"
path = "src/lib.rs"

[features]
default = ["sqlite", "repo-sync", "git"]
sqlite = ["dep:rusqlite"]
repo-sync = ["dep:sha2"]
git = ["dep:git2"]
source-cursor = []
source-claude-code = []
source-codex = []
source-orgii-cli = []
source-orgii-rust-agents = []
schema = []

[dependencies]
core_types = { path = "../types" }
orgtrack_protocol = { path = "../orgtrack-protocol" }
# Leaf crate (no back-edge): managed CLI profile roots for the
# imported-history readers' extra discovery roots.
app_paths = { path = "../app-paths" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { workspace = true }
rusqlite = { workspace = true, optional = true }
sha2 = { version = "0.10", optional = true }
similar = "2.6"
# Diagnostics only: report records this crate skips rather than raises
# (already a transitive dep via `app_paths`).
tracing = "0.1"
prost-reflect = { version = "0.16.3", features = ["serde"] }
regex = "1"
memchr = "2.7"
git2 = { version = "0.20", default-features = false, features = [
    "vendored-libgit2",
], optional = true }

[dev-dependencies]
prost = "0.14.3"
