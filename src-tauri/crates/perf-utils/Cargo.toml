[package]
name = "perf_utils"
version = "0.1.0"
edition = "2021"
publish = false

# Performance-critical leaf utilities (binary detection,
# fast JSON, hashing, diff/patch, process metrics). Pure leaf — no
# back-edges into `app`. Owns ~36 `#[tauri::command]`s, re-registered
# from `commands/handler_list.inc` via bare `perf_utils::…` paths.

[lib]
path = "src/lib.rs"

[dependencies]
app_platform = { path = "../app-platform" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"
tauri = { workspace = true }
tokio = { workspace = true }
tracing = "0.1"
log = "0.4"

# Hashing
sha2 = "0.10"
hex = "0.4"
blake3 = "1.5"

# Binary detection
infer = "0.19"
memchr = "2.7"

# Fast JSON
simd-json = "0.17"

# Diff / patch
similar = "2.6"

# Process metrics
sysinfo = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
webview2-com = "0.38.2"
windows = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_System_ProcessStatus",
  "Win32_System_Threading",
] }
