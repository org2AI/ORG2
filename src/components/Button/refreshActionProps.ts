/**
 * The compact text action used by refresh controls: transparent at rest with
 * a surface background on hover.
 */
export const TEXT_HOVER_REFRESH_BUTTON_PROPS = {
  variant: "tertiary",
  appearance: "solid",
} as const;
