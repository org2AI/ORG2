/**
 * Cloud channel visibility.
 *
 * Named by `store/chatPanel/chatPanelTabsModel` and by the
 * `features/Org2Cloud/channels` wire schemas, so the union lives here.
 */
export type CloudChannelVisibility = "org" | "private";
