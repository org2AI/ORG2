/**
 * Canonical-conversation contracts.
 *
 * Locators, provider targets and the pure guards/key function that define them.
 * Shared by `engines/SessionCore`, `engines/ChatPanel`, `features/Org2Cloud`
 * and `store/ui` — see `src/contracts/README.md`.
 */

export * from "./conversationTypes";
export * from "./queuedConversation";
