import { ShellReplayRangeCache } from "@orgii/replay-core/shell";

/** One payload budget shared by every replay surface in this app instance. */
export const shellReplayRangeCache = new ShellReplayRangeCache();
